# v0.28.0 日常使用体验

## 正式版本

2026-09-14 用户在 dev.3 OTA 就绪后确认发布 v0.28.0。正式版冻结 dev.3 功能源码，
仅调整版本号、发布资料与预览；固定依赖检查、全部主机测试、许可检查和正式构建通过。
OTA 为 2,190,816 字节，与 dev.3 等长，仅 74 字节位于版本、构建信息和镜像校验区域。
分区、模型及 dev.3 的配置布局不变，固件与效果图见[发布目录](../dist/v0.28.0/)。

整体使用反馈不代替网络故障注入、断电写入、闹钟抢占与启动恢复等未单独记录的专项结果；
天气和 AI 对话继续保持 Beta。下方保留候选阶段事实，其中“未提交、未发布、待验证”等
状态仅对应当时阶段，正式发布收据另记在站点发布记录。

## 实施与候选记录

2026-09-14 用户确认实施，随后要求提交、推送与发布等待确认。最新追加要求：已确认实施、
需要实机测试的固件改动，应先完成测试、构建、Beta 部署与公网校验，再通知用户测试；
不自动提交、推送或发布正式版。候选阶段 dev.3 单独部署 Beta。不改变 CPU、Wi-Fi 省电策略、页面停留
规则、Flash 分区或语音模型。

## 范围与安全边界

- 已连接家庭 Wi-Fi 时，设备主动开启临时局域网门户，扫码授权访问；不常驻开放。
  无可用连接、恢复模式或用户主动切换时，使用原 WPA2 随机密码热点。
- 局域网 HTTP 不提供 Wi-Fi 密码、API Key 编辑和本地 OTA；这些敏感维护操作继续使用
  设备热点。局域网可管理普通偏好、RTC、图片与歌曲。所有数据接口均需会话授权，
  授权材料不写日志、不进入 URL 查询参数、不保存在浏览器持久存储。
- 浏览设置或歌单不停止音乐；写卡和固件事务仍取得独占资源并安全停止音乐。
- 普通门户按用户活动计算 5 分钟闲置时间，另设绝对会话与单事务上限；状态轮询不能续期。
  歌曲支持多选、顺序上传，每首独立校验和提交，失败停止队列，不覆盖同名文件。
- 音乐页突出两行歌名，缩小播放状态；播放音量编辑可试听，取消、超时和抢占恢复最新
  已保存音量，只有保存写 NVS。
- 闹钟音量与媒体音量分开，门户提供设置和短试听；迁移沿用原音量，不突然变响。
  闹钟设置统一交给网页，设备只保留响铃时停止或延后。

## dev.3 设置页排版

用户反馈 dev.2 整体体验较好，指出设置页 POWER 靠左不协调，希望字体更清晰。
本轮只调整普通设置页：网页入口与电源信息统一中轴，POWER 在实际状态上方；操作提示
从细等宽小字换成原生 Helvetica 粗体 12，辅助说明使用常规体 12，页脚改为粗体 10。
标题、电源值保留原有层级；不修改主时钟、日期、中文字体、按键、低功耗规则或 OTA。

排版与状态文案集中在 `src/display/settings_display_layout.h`；测试通过真实 U8g2 字体
解码捕获像素，检查所有 32 种电源标志组合的居中、边距、行距和页脚边界。
设置页 SVG 使用同一布局和字库生成，不借用浏览器字体模拟；复现命令：

```sh
./scripts/preview-settings.sh > /tmp/rlcd-settings-preview.svg
```

版本号推进到 `0.28.0-dev.3`，避免重用已经部署的 dev.2 版本和校验值；本地验证后按用户
追加要求部署 Beta，不提交、不推送、不通过 USB 烧录。正式版仍为 v0.27.0。
用户的整体反馈不等于此前所有专项用例已经实测；dev.3 字体的真实反射屏观感还需验证。

### dev.3 本地验证（2026-09-14）

`test.sh`、`check-repository.sh`、`check-licenses.sh`、`build.sh` 全部通过；本机浏览器
按 400 × 300 检查真实字体像素 SVG，电源信息居中、各行无重叠。预览脚本生成结果与
`docs/assets/settings.svg` 逐字节一致。没有下载字库或工具链。

- OTA：2.19 MB（2,190,816 字节），比 dev.2 增加 9,472 字节，主要为复用字库中的字体。
- OTA SHA-256：`4a6c1ae08af4673c809bad8f94c54184286a059999c7adedc777f1fdc321dbec`
- Factory SHA-256：`ef8903784f98a6ddd4545a8dceff2681475fc06e4921d3d6b1076c2a2a5a7fd7`
- 分区表与离线模型 SHA-256 均与 dev.2 一致。本地验证阶段没有操作服务器或设备。

### dev.3 Beta 部署与测试安排（2026-09-14）

使用已验证的同一份构建执行固定流程，没有重新构建或下载工具：

```sh
./scripts/deploy-testing-channel.sh ../esp32-rlcd-firmware/build/rlcd_firmware.bin 0.28.0-dev.3 0.7.0 2026-09-14
```

仅新增 `testing/v0.28.0-dev.3/` 并原子替换 `testing.json`，不覆盖历史候选。
旧指针备份为 `/www/backup/mcu.taifua.com/testing-20260914T142203Z-672ac61f80fe`。
不修改网站前端、正式固件目录、Nginx、Docker 或其他服务，不提交或推送；远程操作顺序执行。

公网清单与 OTA 均与本地包逐字节一致，SHA-256 与上方构建记录相同；稳定清单部署前后
逐字节一致，仍为 v0.27.0。测试清单禁止缓存，版本化 OTA 使用一年不可变缓存。

设备在 `ONLINE UPDATE` 按住 KEY 2 秒，确认目标为 `0.28.0-dev.3`，松开后再按住 3 秒
安装。本次可验证 dev.2 直接刷新到最新候选、不先跳到旧正式版本；更新后检查 SETTINGS 的
电源区居中、小字清晰度、长按打开网页与手动省电。实机结果仍待用户反馈。

## dev.2 交互与 OTA 调整

- Chat 加入 BOOT 日常页面环，位于可选音乐之后；系统中心收敛为设置、在线更新、状态。
- 设置页长按 KEY 2 秒直接打开网页；移除设备端设置列表，保留省电开关与音乐页音量。
- 网页按常用、联网、媒体、维护切换，保留未保存输入；常用、闹钟、更新三个表单独立保存，
  后端限制各自字段范围；完整旧表单保持兼容，存储布局不再变化。
- 局域网授权、敏感配置的热点限制、音乐事务安全与恢复模式不变。
- 手动检查先刷新清单，有新版便直接进入安装确认；目标发生变化时展示新目标并重新确认。
  清单被拒绝不会误报成功或重启，更新任务完成资源释放前不接受新任务或切换通道。
- 仅按本轮确认部署 Beta，不自动提交、推送、发布或通过 USB 烧录。

### dev.2 本地验证（2026-09-14）

`test.sh`、`check-repository.sh`、`check-licenses.sh`、`build.sh` 通过。
新增测试覆盖全部 8 种可选页面组合、Chat 的实际渲染入口、设置长按直达与松手门控、
音乐音量取消恢复、三种独立表单的字段限制、真实 HTML 结构与草稿保留。
使用已安装的本机 Chromium 检查了 320 / 390 像素宽度的分区布局；页面使用模拟数据，
不等于目标板或手机实机验收。没有下载新工具链，没有提交、推送或通过 USB 操作设备。
OTA 服务测试运行真实的更新任务、清单解析和镜像检查代码，模拟网络、Flash 与任务调度，
覆盖过期正式目标刷新为最新开发版、dev.1 直升 dev.2、安装前目标变化、同版/旧版、错误
通道/硬件/最低版本/摘要、HTTP 失败、镜像/写入/校验/启动分区失败、取消、任务创建失败与
资源释放期间的重入。哈希模拟仅验证控制流程，不代替 ESP-IDF 加密库或实机升级验证。

- 版本：`0.28.0-dev.2`，基于 `0f35eea8e4cb28011f4a732f36cab98fe54dd51e` 加本地未提交改动。
- OTA：2.18 MB（2,181,344 字节），3 MiB 应用分区剩余约 31%。
- OTA SHA-256：`084bbd1ba170210bf96ec629d2e7ab59836bb072d467c7ca814639df83f015f0`
- Factory SHA-256：`360f26da2425905c1e54349e0e5c81d2e4483ac45b9669bd86ae4c408f84eb25`
- 分区表与离线模型哈希未变，正常升级仅需 OTA 应用。

实机待确认：BOOT 可到 Chat 并正常对话；KEY 系统页只剩三页；
设置页长按 2 秒直接打开网页；四区切换与独立保存正常；恢复门户与本地 OTA 仍可使用。

### dev.2 Beta 部署与测试安排（2026-09-14）

用户授权后，在相邻站点仓库使用固定流程执行：

```sh
./scripts/deploy-testing-channel.sh ../esp32-rlcd-firmware/build/rlcd_firmware.bin 0.28.0-dev.2 0.7.0 2026-09-14
```

仅新增 `testing/v0.28.0-dev.2/` 并原子替换 `testing.json`；旧指针备份为
`/www/backup/mcu.taifua.com/testing-20260914T140239Z-672ac61f80fe`。不覆盖 dev.1，
不修改正式发布、网站前端、Nginx 或 Docker；服务端操作顺序执行。

公网清单与 OTA 均与本地打包逐字节一致；稳定清单部署前后逐字节一致，仍为 v0.27.0。
两个通道清单均返回禁止缓存响应头，带版本的 OTA 返回一年不可变缓存。

本次升级先在旧固件核对 `BETA LATEST` 为 `0.28.0-dev.2` 再确认安装。旧固件若仍缓存
上次的正式目标，可在未开始写入时正常重启，再检查新版，无需先升级中间正式版。
安装成功后验证以上交互；再次手动检查应显示已是最新版本。保持 Beta 设置不变，下一候选
部署后再验证跨候选直升。不要为了测试修改线上清单、降级或在写入期间断电。

### OTA 问题说明

旧版确认页直接使用最近一次检查的内存缓存，安装任务才重新获取服务器清单。设备曾检查
到测试通道指向正式版、随后服务器又发布开发版时，会先展示旧正式目标，安装前因版本、
URL、大小或哈希不同而拒绝；这不是 OTA 只能逐版升级，也不是服务器未禁止缓存。
本次将手动刷新放到确认前，同时保留写入前复核；有效目标变化只要求重新确认，不静默
换包。测试通道仍由用户设置决定，不根据当前版本的 `dev` 后缀擅自切换。

另外修正安装任务只检查 HTTP 成功、未同时检查策略结果的分支：只有真实镜像写入、
校验、OTA 结束和启动槽选择全部成功，才显示成功并重启。

## 升级兼容

偏好记录升级为 schema v8，写入新的 `cfg8_a` / `cfg8_b` 双槽，不覆盖旧记录；迁移保留
时区、温标、媒体音量、闹钟规则、省电偏好和更新通道，闹钟音量继承媒体音量（含静音）。
Wi-Fi、天气、AI 凭据使用各自独立存储，不修改其布局。不更改 Flash 分区或语音模型，
已有 v0.7.0+ 完整安装可以通过 OTA 更新应用。

回退到仅支持 schema v7 的旧固件时，它会使用安全默认偏好并保护新记录，不等于新偏好
丢失；重新升级可恢复 v8 保存值。Wi-Fi 与 API 凭据不随偏好回退清除。

局域网令牌每次开启更新，浏览器只在内存保留，刷新后重新授权。访问码错误最多尝试
8 次，间隔至少 1 秒；超过后仍可扫码，或重新开启门户。HTTP 不是加密连接，仅限可信
家庭局域网，不做路由器端口映射，不把访问码或二维码发到公共平台。

## 完成检查

- [x] 设置记录迁移、独立闹钟音量及主动试听。
- [x] 音乐页、音量试听与取消恢复。
- [x] 门户闲置/事务超时、歌曲顺序上传。
- [x] 临时局域网授权、热点回退、接口访问范围与资源释放。
- [x] 主机测试、真实门户脚本测试、仓库与许可检查、ESP-IDF 构建。
- [x] 更新设计规范、使用文档、预览与候选部署记录。
- [ ] 实机：同 Wi-Fi、热点、无网、无卡、多首上传、闹钟抢占、音量取消、本地与在线 OTA。

主机测试不能代替目标板的长时间播放、异常断电和网络兼容性验证。功耗基线、倒计时、
音乐定时停止及唤醒词不混入此版本。

## 候选构建记录

2026-09-14，`0.28.0-dev.1` 使用现有 ESP-IDF v5.5.3 和固定依赖构建，无需临时下载工具。
`bootstrap.sh --check`、`check-repository.sh`、`check-licenses.sh`、`test.sh`、`build.sh`
及相邻站点的测试通道打包检查通过。OTA 为 **2.18 MB**（2,179,392 字节），3 MiB 应用
分区仍有约 31% 余量；Factory 为 8,560,811 字节。

- OTA SHA-256：`0a33740b6694d038f9b02e7a41ae444bafc15c02ae41f94c12b8706e23e20b1e`
- Factory SHA-256：`792df9a815620005dedbf5e1cd1a573867e07e53b2ab57eb1e0983cb13355a9b`
- 语音模型 SHA-256：`29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`，与 v0.27.0 一致。

主机运行真实门户内嵌脚本，覆盖草稿保护、闹钟试听提交、多文件队列、失败保留、访问码与
二维码授权、局域网凭据提交拦截；C 测试覆盖计时回绕与时限、Host 与路由限制、迁移提交
失败重试、独立音量、取消恢复、告警优先级、二维码尺寸和真实字体页脚宽度。
这些测试不等于手机浏览器、ESP32 HTTP 服务、音频抢占或异常断电的实机验证。

已通过相邻站点固定的 `deploy-testing-channel.sh` 部署到 **Beta 通道**。
设备开启 Beta 更新后，在 `ONLINE UPDATE` 检查并确认安装 `0.28.0-dev.1`，无需 USB。
公网清单、OTA 与本地构建逐字节一致；测试清单禁止缓存，版本化 OTA 使用长期不可变缓存。
稳定清单部署前后逐字节相同，仍为 v0.27.0；没有更新网站页面、正式 Release、Nginx 或 Docker。
旧测试指针备份编号：`testing-20260914T130236Z-5430f0aa2cde`。
本轮未向目标板写入，以下实机验收由实际升级后的观察补齐。

## 实机验收顺序

1. Beta 在线安装后核对版本与原设置；首屏、天气、图片、AI 与离线语音均可正常使用。
2. 家庭 Wi-Fi 已连接时打开网页设置，手机保持同一网络扫码；关闭后重新开启，旧访问码
   失效。直接输入 IP 时需访问码；未授权不能读取设置、歌曲和图片。
3. 局域网中试普通设置和歌曲管理；API Key、Wi-Fi 编辑及本地 OTA 禁用。
   短按设备 `KEY` 切回热点，凭据编辑与本地 OTA 仍可用；普通关闭后恢复家庭连接。
4. 播放中打开门户和刷新歌单，声音不中断；选择多首歌曲，上传顺序、数量、进度正确。
   中断第二首上传，重新打开后第一首保留，半文件不出现，不影响原有文件。
5. 音乐页显示两行长歌名。按住 `BOOT` 2 秒调音量，试低档位、静音、取消、保存和
   30 秒无操作；取消或超时应恢复已保存音量，保存后重启仍保留。
6. 媒体音量为 0，独立闹钟音量设为适中值并保存，到点应有声。闹钟音量为 0 则只显示
   提醒。试听不保存，真实闹钟可抢占试听；按键停止后不能迟到再次响起。
7. 关机取卡后开机，无卡提示不影响时钟与设置；断开路由器或启用省电后门户仍走热点。
   不热插拔，不用正在写入的卡做断电试验。空闲 5 分钟关闭，有操作可续期但不超过
   30 分钟；恢复门户仍保留精简功能和本地 OTA。
