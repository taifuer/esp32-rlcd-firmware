# 实机 Bring-up 与操作记录

本文件保存可复用的硬件事实和已经发生的关键操作。日常操作以
[开发烧录指南](flashing.md)为准。

## 硬件基线

| 项目 | 实测值 |
| --- | --- |
| 开发板 | Waveshare ESP32-S3-RLCD-4.2 |
| 芯片 | ESP32-S3 QFN56 rev 0.2 |
| Flash | 16 MB |
| PSRAM | 8 MB Octal PSRAM（AP，80 MHz） |
| Windows 端口 | COM5（可能随系统变化） |
| USB 标识 | `303a:1001`，USB Serial/JTAG |
| RTC | PCF85063，I2C `0x51` |
| 温湿度 | SHTC3，I2C `0x70`，ID `0x0887` |

## 2026-08-17：外部固件验证硬件

- 曾烧录 Weather Clock v1.5.37，以确认屏幕、音频和配网硬件可以工作。
- 该外部固件只保存在 Git 仓库外作为回退资料，不属于本项目源码或发布物。
- 随后转入由本项目维护的原生 ESP-IDF 固件路线。

## 2026-08-17：项目固件 v0.1.0

- 完成 ST7305 屏幕、8 MB PSRAM、PCF85063 和 SHTC3 的最小离线诊断。
- 实机启动成功，RTC 因首次使用/掉电标志显示 `RTC TIME IS NOT VALID`。
- 温湿度读取正常，SHTC3 ID 为 `0x0887`。
- Windows COM5 直接烧录可靠；WSL USBIP 因 `VDI_USB_HUB_FILTER` 不作为默认方案。

## 2026-08-17：项目固件 v0.1.1

### 烧录

- 目标确认：VID/PID `303a:1001`，ESP32-S3 rev 0.2，8 MB PSRAM。
- 完整合并镜像写入地址：`0x0`。
- 写入范围：`0x00000000` 至 `0x0005bfff`。
- 写入字节数：373,072。
- esptool 报告：`Hash of data verified.`
- 合并镜像 SHA-256：
  `5f8a4751fd0a72886ba7f9b344cfdf642afc7246f1401eeff48a6cd8cde8768d`。

### 启动与校时

- 启动日志确认 `App version: 0.1.1` 和 ESP-IDF v5.5.3。
- 8 MB PSRAM 内存测试通过。
- PCF85063 与 SHTC3 均被检测到。
- 发送：`SET_TIME 2026-08-17 22:33:20`。
- 回执：`RTC_SET_OK 2026-08-17 22:33:20 weekday=1`。
- 三秒后读取：`RTC 2026-08-17 22:33:23`，证明 RTC 正常递增。
- 屏幕实机确认可见 `Hello, world.`。

### 复位观察

- 普通 USB `hard-reset` 曾让芯片继续停在 Bootloader。
- Watchdog reset 可以启动应用，但 Windows 端口重枚举可能让 esptool 返回串口异常。
- 因此标准流程固定为：烧录后物理关机，再在不按 BOOT 的情况下正常开机。

## 2026-08-18：项目固件 v0.2.0 首屏与发布

### 实机候选构建

- ESP-IDF v5.5.3 完整构建和主机测试通过。
- 应用镜像大小：633,968 bytes；SHA-256：
  `3798052cbcf9316100a83df55277f468741d960c059e79317b422631441cd1d6`。
- 完整合并镜像大小：699,504 bytes；SHA-256：
  `60d11f05df19c6cdf2d7e267dea25ffd7e4ff4bffdd137a6c0055515cf87c0b4`。
- 完整镜像从 `0x0` 写入，esptool 报告 `Hash of data verified.`。
- 启动日志确认候选版本 `App version: 0.2.0-dev`。

### 功能验收

- RTC 写入 `2026-08-18 21:31:09` 后回读 `21:31:12`，秒数正常递增。
- SHTC3 实测约 31.3 °C、73.0% RH，传感器状态正常。
- GPIO4 电池 ADC 使用校准数据，实测约 4,125 mV、93%。
- 8 MB Octal PSRAM、PCF85063、SHTC3 和 ST7305 均正常初始化。
- 用户实机确认最终首屏基本居中、显示效果良好。

### 最终首屏布局

- 顶部 56 像素：公历日期、无前缀农历、中文星期和电池组等间距排列。
- 中部 188 像素：`HH:MM:SS` 使用同一 78 像素字体，字形视觉中心位于整屏
  `y=150`。
- 底部 56 像素：温度和湿度使用 20 像素字体，分别在左右半屏居中，以静态实心心形
  分隔。
- 首屏不显示版本、I2C 地址、传感器 ID、硬件状态或调试文本。

### 正式发布构建

- 实机验收后，功能源码保持不变，仅将项目版本从 `0.2.0-dev` 更新为 `0.2.0`。
- 再次执行依赖检查、全部主机测试和 ESP-IDF 完整构建，结果通过。
- 正式应用镜像大小：633,968 bytes；SHA-256：
  `cecd4ee4c9a8a403dccbe8b97b3798a7f6922e32d18d121eb2c9955d431f7374`。
- 正式合并镜像大小：699,504 bytes；SHA-256：
  `d034c397aba9f79985bc7c187c24fb8db4e974b6b7ca7c0dcd0b724e1a21e389`。
- 完整发布文件保存到 `dist/v0.2.0/`，烧录地址为 `0x0`。

## 2026-08-19：项目固件 v0.3.0 配网、网络校时与发布

### 实机候选构建

- ESP-IDF v5.5.3 完整构建、许可检查和全部主机测试通过。
- 修正 Wi-Fi 二维码兼容性后的开发应用镜像大小：1,263,392 bytes；SHA-256：
  `aaeba55876b60962655a370ec653724ca041f5526cbb3ec29f972db3a2e720da`。
- 开发完整合并镜像大小：1,328,928 bytes；SHA-256：
  `55a1ec716f814ac6f544a38d62c817e97eadea06419130b6a225534c0f944ac6`。
- 完整镜像从 `0x0` 写入到 ESP32-S3 rev 0.2，esptool 报告
  `Hash of data verified.`。

### 功能验收

- 空白 NVS 启动后显示 `WI-FI SETUP`、唯一临时热点名称、8 位随机密码、二维码和
  `http://192.168.4.1` 后备地址。
- 小米 Android 手机扫描修正后的无额外双引号 Wi-Fi 载荷，可以加入临时 WPA2 热点；
  其他 Android 机型与 iOS 尚未逐一实机验收。
- 本地配网页面、家庭 2.4 GHz Wi-Fi 连接、SNTP 自动校时和 PCF85063 写入正常；时间
  显示正常，首屏 Wi-Fi 状态图标可见。
- 首屏保持三段式布局，加入 Wi-Fi 状态后日期、农历、星期、电池和大时间仍然对齐。
- 演示图纠正为实机显示极性：黑色背景、白色像素。

### 正式发布构建

- 实机验收后仅把项目版本从 `0.3.0-dev` 更新为 `0.3.0`，再执行完整发布检查。
- 正式应用镜像大小：1,263,392 bytes；SHA-256：
  `f2754906541c41e7f751d3dd3ef58460868435c36c6ed1392e8647d20d852c9c`。
- 正式合并镜像大小：1,328,928 bytes；SHA-256：
  `0b9705797c3f36e7e1cb84b654daad74fbeecd6d5800672c244c7d4d2ba139c8`。
- 完整发布文件保存到 `dist/v0.3.0/`；许可检查、主机测试、ESP-IDF 完整构建、版本附件
  打包和全部 SHA-256 回读校验通过。

## 2026-08-20：项目固件 v0.4.0 按键维护与状态页

### 构建与烧录

- 按键状态机主机测试、仓库检查、许可检查和 ESP-IDF v5.5.3 完整构建通过。
- 应用镜像大小：1,266,400 bytes；SHA-256：
  `f7be07fe255e9cadcfb356fe13be95cbc60f7679826e3046f027c17171e71c74`。
- 完整合并镜像大小：1,331,936 bytes；SHA-256：
  `315ddf545ad328ba85326e526e290dcdc30e9b1e95a38fdf55052dfd8006c808`。
- 在 Windows `COM5` 核对 `303a:1001` 后写入 ESP32-S3 rev 0.2；esptool 报告
  `Hash of data verified.`。

### 功能验收

- 板载 `KEY`（GPIO18，低电平有效）短按可进入设备状态页，再次短按可返回首屏；无人操作
  15 秒自动返回正常。
- 状态页的固件、RTC、温湿度、电池、网络和最近校时信息显示正常，布局实机确认可用。
- 按住 `KEY` 约 2 秒后松开，倒计时取消且 Wi-Fi 配置保持不变；持续满 5 秒后只清除项目
  Wi-Fi 凭据并自动重启进入配网。
- 完成长按重配网后，二维码、家庭 2.4 GHz Wi-Fi、SNTP 自动校时和首屏时间均恢复正常。
- `BOOT` 保持只用于 ESP32-S3 ROM 下载模式，不参与运行时按键交互。

## 2026-08-20：项目固件 v0.5.0 月历、固件信息与 BOOT 操作

### 实机候选构建与烧录

- 主机测试、仓库补丁检查和 ESP-IDF v5.5.3 完整构建通过。
- 开发完整合并镜像大小：1,341,088 bytes；SHA-256：
  `0bd939fd2fcfd5b86f22d08be4b7509ce79ba8eab891573480f1339452a7adde`。
- 在 Windows `COM5` 核对 `303a:1001` 和 ESP32-S3 rev 0.2 后，从 `0x0` 写入完整镜像；
  esptool 报告 `Hash of data verified.`。

### 功能验收

- 正常运行时短按板载 `BOOT`，可按“首屏 → 当月月历 → 固件信息 → 首屏”循环；月历
  使用星期一为每周首日，实心圆正确突出 2026 年 8 月 20 日，并显示七月初八、星期四
  和当前时间。
- 固件信息页显示当前版本、USB 下载模式按键步骤和项目最新 Release 二维码，手机扫码
  可正常打开目标地址。
- 月历与固件信息页无操作 30 秒可自动返回首屏；`KEY` 状态页、返回和长按 5 秒重配网
  行为保持正常。
- 正常运行时按住 `BOOT` 约 2 秒可唤醒已有网络任务完成即时 SNTP 校时，RTC 写入回读、
  结果提示和返回页面均正常。
- 关机后按住 `BOOT` 再按 `PWR` 仍可进入 ESP32-S3 ROM 下载模式；应用运行时的按键功能
  不改变硬件下载方式。

### 正式发布构建

- 实机验收后，功能源码保持不变，仅将项目版本从 `0.5.0-dev` 更新为 `0.5.0`，随后再次
  执行依赖、许可、主机测试和 ESP-IDF 完整构建检查。
- 正式应用镜像大小：1,275,552 bytes；SHA-256：
  `fcfed8e730d85f41f9138d3161157fa6ef3c09e4b8fcea14dd5b152572c15885`。
- 正式完整合并镜像大小：1,341,088 bytes；SHA-256：
  `6d68aa6ba3eb17fd6c2fc16cd064c1ac173de62790ab8d29f0649cbd5c3a3c00`。
- 完整发布文件和对应界面效果图保存到 `dist/v0.5.0/`，烧录地址为 `0x0`。

## 2026-08-20：项目固件 v0.6.0-dev 信息架构与系统中心

### 候选构建与烧录

- 仓库结构与文档链接、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 完整构建通过。
- 开发应用镜像大小：1,277,408 bytes；SHA-256：
  `dc0b27732efb840939914323c29da8baccec130c348b7bfaeae951d5b75397f2`。
- 开发完整合并镜像大小：1,342,944 bytes；SHA-256：
  `7e612cbbd889bc8db3cd4ad033c3067d9ef87ac14c217a1298bb764cc95ba8bb`。
- 在 Windows `COM5` 核对 `303a:1001`、ESP32-S3 rev 0.2 和 8 MB PSRAM 后，从 `0x0`
  写入完整镜像；esptool 报告 `Hash of data verified.`。

### 功能验收

- 完整镜像覆盖 NVS 后可重新完成配网、自动校时并恢复正常首屏；用户确认当前功能正常。
- `BOOT` 日常功能路径和 `KEY` 系统中心路径可正常使用；系统中心按设备健康、网络与时间、
  Wi-Fi 维护、关于与更新四页组织，不再把版本、诊断和维护操作混入日常页面。
- 页面采用统一标题、页码、内容网格和底部操作提示；README 预览同步为每行两张的
  400 × 300 黑底白字效果图。
- 关机后按住 `BOOT` 再按 `PWR` 会直接进入芯片 ROM 下载器，应用不会运行，因此无法
  动态绘制 USB 烧录进度；本次进入该模式时观察到屏幕为空白。后续若需要烧录提示，应
  利用全反射屏保留进入下载模式前的“关于与更新”画面，不伪造实时进度。

### 正式发布构建

- 实机验收后，功能源码保持不变，仅将项目版本从 `0.6.0-dev` 更新为 `0.6.0`，随后再次
  执行依赖、许可、主机测试和 ESP-IDF v5.5.3 完整构建检查。
- 正式应用镜像大小：1,277,408 bytes；SHA-256：
  `c687406337fa83f6b24e108e01a5811b1ce2992aeebebe1ef8f4f6633a9a1c68`。
- 正式完整合并镜像大小：1,342,944 bytes；SHA-256：
  `85db4c11df52da261a2763245e90d5d3eb7a42a1e65606d2bd1433dc134a4820`。
- 完整发布文件和六张对应界面效果图保存到 `dist/v0.6.0/`，烧录地址为 `0x0`。

## 2026-08-21：项目固件 v0.7.0 本地 OTA 与 RTC 备用电池判定

### Factory 迁移与升级入口

- 首次切换到双 OTA 分区时，在 Windows `COM5` 核对 `303a:1001`、ESP32-S3 rev 0.2 后，
  将 v0.7.0-dev Factory 合并镜像从 `0x0` 写入；写入 1,362,288 bytes，SHA-256 为
  `390f4fdddbf9a657410609cc1adde908f931384be96e32b0c74b0d8f97b37190`，esptool 报告
  `Hash of data verified.`。
- Factory 迁移覆盖 NVS 后重新完成一次配网，首屏、时间和原有页面正常；“关于与更新”页
  长按 `KEY` 3 秒可显示随机密码升级热点、二维码和固定入口，等待上传时短按 `BOOT` 可
  安全取消并返回。

### 本地 OTA 与配置保留

- 通过手机连接升级热点并上传 1,299,200 bytes 的 v0.7.0-dev OTA 候选镜像，SHA-256 为
  `ba47d26c25121fcb0efab220140623354a6c3afbd231d5d97dc7940ad385f564`；屏幕接收百分比、
  校验、完成和自动重启流程与设计一致。
- OTA 重启后家庭 Wi-Fi 凭据保留，设备自动连接并校时，无需再次扫码输入密码；设备健康
  页新增 `RTC BACKUP` 行，首次显示 `UNTESTED`。

### RTC 备用电池实机判定

- 保存测试标记后拔掉 Type-C、长按 `PWR` 关机并等待，再由 18650 正常开机；首次测试在
  NTP 启动前显示 `FAILED`，随后设备使用已保存的网络配置自动联网恢复时间，失败结果没有
  被 NTP 覆盖。
- 确认备用电池为兼容的 `ML1220-1.0`，正常供电充电后再次完成真实断电测试，状态由
  `FAILED` 自动恢复为 `VERIFIED`，证明停振标志、跨启动标记和恢复路径均按预期工作。
- 本次实机覆盖 Factory、网页 OTA、配置保留和 RTC 三种状态；故意不确认镜像的 Bootloader
  回滚及 `update-app.sh` 串行后备路径未单独执行实机测试。

### 正式发布构建

- 实机验收后将项目版本更新为 `0.7.0`，再次使用 ESP-IDF v5.5.3 完成依赖、许可、主机
  测试和完整构建检查。
- 正式 Factory 镜像大小为 1,364,736 bytes，SHA-256 为
  `112e1a0eb05f83af2d6cda46863c89ba0736f967963120c4c26c1eb0d378d67b`；从 `0x0`
  写入，用于首次安装、旧分区迁移和恢复。
- 正式 OTA 镜像大小为 1,299,200 bytes，SHA-256 为
  `9da007246a680020f706dce96ba5ab990d9d6edfb13d2e4c4c44704a790bc652`；通过设备本地升级
  页面写入，用于 v0.7.0 及后续版本之间的日常升级并保留 NVS。
- 两类固件、校验文件和八张对应界面效果图保存到 `dist/v0.7.0/`。

## 2026-08-21：项目固件 v0.8.0 音频诊断与临时回放

### 实现与构建

- 依据微雪固定提交中的板级配置接入 ES8311 扬声器与 ES7210：24 kHz、16-bit，输出使用
  标准 I2S，输入使用四时隙 TDM；板载两个物理麦克风按 MIC1/MIC3 对应时隙 0/2 读取。
- 系统中心扩展为五页，新增“音频”页；首个候选长按 `KEY` 2 秒播放两声算法生成的提示
  音，随后采集 1.6 秒麦克风数据并显示去直流 RMS 相对强度。
- 第二个候选把测试改为后台状态机：临时采集最多 5 秒双麦克风语音，实时显示两路强度，
  自动选择更强的一路回放；录制/回放可用 `KEY` 提前结束，活动阶段可用 `BOOT` 取消。
  480,000 bytes 原始语音只分配在 PSRAM，播放、取消或失败后覆写并释放。
- 外部 `esp_codec_dev` 源码继续位于 Git 仓库外；项目自有构建适配只编译 ES8311、ES7210
  及其必要接口，不引入上游可选 codec。
- 外部依赖、仓库边界、许可材料、全部主机测试和 ESP-IDF v5.5.3 完整构建通过。
- 第二个实机候选的开发 OTA 镜像大小为 1,359,248 bytes，SHA-256 为
  `13d6b44d3d8803391f09736a226fc69ec2e26de09c35e75633df7998c9916877`；Factory 合并镜像
  大小为 1,424,784 bytes，SHA-256 为
  `b820d63ad456757440237e847d6698c94216061f8ed601e6c90102fcb248c2f8`。
- 音量微调候选的开发 OTA 镜像大小为 1,359,264 bytes，SHA-256 为
  `0a3ecfa28e6f4e0b746ede103bafa007b848d82b6eaf321c74139e6b114d9111`；Factory 合并镜像
  大小为 1,424,800 bytes，SHA-256 为
  `e495bc96d3c6b21fb1bb8a0d6589a88b8be07a03bee73306351d9abe7af0d5da`。

### 实机验收

- 通过 v0.7.0 的本地升级页面写入 `0.8.0-dev` OTA 候选，设备成功启动并可进入新增的
  “音频”页。
- 用户确认长按 `KEY` 2 秒后可以听到两声提示音，随后页面可以检测到麦克风输入，证明
  ES8311 输出和 ES7210 采集基础链路可用。
- 写入第二个 OTA 候选后，用户确认最长 5 秒录制、`KEY` 手动停止、`BOOT` 取消、自动
  语音回放及各页面状态均正常；回放人声可辨识但音量稍小。
- 后续候选保留已经确认合适的双音音量，仅把语音回放从 `esp_codec_dev` 音量 58 调至
  68，约提高 5 dB；数字峰值限幅和淡入淡出保持不变，避免用数字削波换取响度。该微调
  经第三次本地 OTA 后完成实机复核，用户确认音量效果可用，其他功能仍正常。两路麦克风
  强度尚未做声学定量校准，也未执行长时间音频稳定性测试。

### 正式发布构建

- 实机验收后，功能源码保持不变，仅将项目版本从 `0.8.0-dev` 更新为 `0.8.0`；固定依赖、
  仓库边界、许可材料、全部主机测试和 ESP-IDF v5.5.3 正式构建再次通过。
- 正式 OTA 镜像大小为 1,359,264 bytes，SHA-256 为
  `4d3c173e47d51624f969cbaf9ff3c15045a5d95896ac08906ac007cb873ee3c2`；通过设备本地升级
  页面写入，用于 v0.7.0 及后续版本之间的日常升级并保留 NVS。
- 正式 Factory 镜像大小为 1,424,800 bytes，SHA-256 为
  `04427a66bc218e81ef2c5f9ad32665a42ec50326168a85aba4713d4d34d8c38e`；从 `0x0` 写入，
  用于首次安装、旧分区迁移和恢复。
- 两类固件、校验文件和对应界面效果图保存到 `dist/v0.8.0/`。

## 2026-08-22：项目固件 v0.9.0 离线优先网络行为

### 实现与候选构建

- 自动 Wi-Fi、NTP 和失败重试不再覆盖日常页面；RTC 有效时继续正常显示时间与月历，RTC
  无效时首屏保持相同布局并显示 `--:--:--`，温湿度、电量、系统中心和音频不受影响。
- 已保存网络连接失败后不再自动开放配网热点或清除凭据；失败阶段区分为 Wi-Fi、NTP 与
  服务错误，后台退避间隔依次为 1、5、15、60 分钟，后续保持每小时尝试一次。
- 首次或主动重配网的热点最多开放 5 分钟；二维码 60 秒后返回首屏，也可按屏幕提示的
  `BOOT: OFFLINE` 立即跳过。没有家庭网络配置时，窗口结束后仍可使用本地 OTA 热点。
- 新增可在主机运行的退避策略和配网页覆盖策略测试；固定依赖、仓库边界、许可材料、全部
  主机测试和 ESP-IDF v5.5.3 完整构建通过。
- `0.9.0-dev` OTA 候选大小为 1,359,744 bytes，SHA-256 为
  `cd6ed7e040bac51e3bc24b57bb68c60ea0ce3ef57b59d52f8e5c118fbf0f40c7`；Factory 合并镜像
  大小为 1,425,280 bytes，SHA-256 为
  `8ca095d895a928cec0cb4826525a52b22b190eb4f8a0235c436f32a0839c3f43`。

### 实机验收

- 通过保留 NVS 的串行应用更新写入上述 OTA 候选；esptool 核对 ESP32-S3 rev 0.2、8 MB
  PSRAM，完成 1,359,744 bytes 写入并报告 `Hash of data verified.`，随后只清除 `0xd000`
  的 8 KiB OTA 选择数据。设备从 `ota_0` 启动 `0.9.0-dev`，原 Wi-Fi 配置与 RTC 判定均
  保留，RTC 备用电池继续显示 `VERIFIED`。
- 用户随后主动执行 Wi-Fi 重置并不提交新配置：设备按预期先显示首次配网页；退出后正常
  显示 RTC 日期与时间，首屏网络图标为离线状态，证明空白网络配置不会阻止离线首屏。
- 保持空白网络配置运行超过 5 分钟后，配网页未再次抢占首屏，RTC 时间持续正常；临时
  `ESP32-RLCD-*` 配网热点按期关闭，首次配网窗口超时策略通过实机验证。
- 随后重新提交家庭 Wi-Fi 配置，设备自动联网并完成 NTP 校时；再次正常重启后使用 NVS
  中保存的配置恢复联网，没有重新进入配网页，离线到联网的完整恢复路径通过实机验证。
- 保存凭据但路由器不可达时的完整后台退避、家庭 Wi-Fi 可达但互联网/NTP 不可达，以及
  离线本地 OTA 入口尚未分别做专项实测；这些场景共用已经通过主机测试的状态策略，不影响
  本次离线基础体验验收。

### 正式发布构建

- 实机验收后功能源码保持不变，仅将项目版本从 `0.9.0-dev` 更新为 `0.9.0`；固定依赖、
  仓库边界、许可材料、全部主机测试和 ESP-IDF v5.5.3 正式构建再次通过。
- 正式 OTA 镜像大小为 1,359,744 bytes，SHA-256 为
  `697cbef186e9925005e27dfbec1fa89c6e358742f0240588c7c9d32b93d68acd`；用于 v0.7.0 及后续
  版本之间的日常升级并保留 NVS。
- 正式 Factory 镜像大小为 1,425,280 bytes，SHA-256 为
  `158e41d6201f92811c8f09f025766cdbd010eb0c7d923e3878d5e0d12cc0a6a6`；从 `0x0` 写入，
  用于首次安装、旧分区迁移和恢复。
- 两类固件、校验文件和对应界面效果图保存到 `dist/v0.9.0/`。

## 2026-08-22：项目固件 v0.10.0 在线更新候选

### 候选构建与测试通道

- 首次启用在线更新客户端的 `0.10.0-dev` OTA 候选大小为 1,568,768 bytes，SHA-256 为
  `95ba832200df8039a4ac6229ea422f05e37fc6568e851e360b8938a19eda4301`；先通过设备既有的
  本地更新页面安装，用于建立后续在线更新能力。
- 在线更新目标 `0.10.0-rc.1` OTA 候选大小同为 1,568,768 bytes，SHA-256 为
  `668316b4e4e3a3a63d891b3d7d18751e36dec19c03407af4f6c22d143eb841d3`。
- 预发布设备读取
  `https://mcu.taifua.com/esp32-rlcd/firmware/testing.json`；本次清单指向版本化地址
  `https://mcu.taifua.com/esp32-rlcd/firmware/testing/v0.10.0-rc.1/esp32-rlcd-firmware-v0.10.0-rc.1-ota.bin`，
  清单中的大小与 SHA-256 均与本地候选一致。

### 在线更新实机验收

- `0.10.0-dev` 正常启动后进入 `ONLINE UPDATE`，按住 `KEY` 2 秒从测试清单检查到
  `0.10.0-rc.1`；再次按住 2 秒进入 `REVIEW`，页面正确显示当前版本与目标版本。
- 在确认页按住 `KEY` 3 秒后，设备完成家庭 Wi-Fi 连接和 HTTPS 下载；屏幕依次显示实际
  下载百分比、镜像校验、更新就绪和自动重启，没有要求重新输入 Wi-Fi 配置。
- 重启后设备显示并运行 `0.10.0-rc.1`。用户确认更新页面、系统页面、实体按键和现有功能
  均正常，证明“本地更新安装客户端 → 在线检查 → 明确确认 → 下载写入 → 校验 → 重启”
  的完整正常路径可用。

### 验收边界与发布状态

- 本次没有在实机强制制造重定向、证书错误、错误 SHA-256、响应截断、同版本、降级、下载
  中断或写入期间断电；这些拒绝与回滚边界仍由主机逻辑测试、ESP-IDF 镜像校验和双槽
  Bootloader 策略覆盖，后续仅在有明确恢复方案时再做破坏性验收。
- `0.10.0-dev` 与 `0.10.0-rc.1` 都是测试通道候选，尚未加入 `dist/`、创建正式标签或更新
  `stable.json`，因此本记录不表示 v0.10.0 已正式发布。

### 正式发布构建

- 实机验收后在线更新与其他功能源码保持不变，仅将默认项目版本正式化为 `0.10.0`；正式
  版本由相同固定依赖重新构建，并按版本规则读取 `stable.json`。
- 正式 OTA 镜像大小为 1,568,768 bytes，SHA-256 为
  `27357fccc04d836dcdbaac86e9d888b83cf039586d798c88da69531c195452b0`；正式 Factory
  镜像大小为 1,634,304 bytes，SHA-256 为
  `ad699baf4e774709c3411eb68fc427a2f12021a5c7345ae29bc5f51d6d6e8024`。
- 正式 OTA 与已实机验收的 `0.10.0-rc.1` OTA 大小完全一致。逐字节比较仅有 75 bytes
  不同，全部位于 ESP 应用描述中的版本、构建时间、ELF 摘要，以及由这些元数据引起的
  镜像校验字节与末尾 SHA-256；应用代码、配置、分区布局和其他镜像内容一致。
- 固定依赖检查、仓库边界、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 正式构建通过；
  两类正式固件、校验文件与本版本效果图保存到 `dist/v0.10.0/`。

## 2026-08-23：项目固件 v0.11.0 设置中心

### 候选构建与测试通道

- 先通过固定的 `update-app.sh` 流程安装 `0.11.0-dev` 基础候选，在不清除 NVS 的前提下
  建立设置中心与开发者测试通道；后续候选只发布到隔离的 `testing.json`，稳定通道继续
  保持 v0.10.0。
- 最终实机候选 `0.11.0-dev.2` OTA 大小为 1,588,144 bytes，SHA-256 为
  `951c70aede32e836cd0560011365300dad75243974e97c11fff25cb871173b09`；线上清单与实际下载
  文件的版本、大小和 SHA-256 均与本地构建一致。

### 实机验收

- 系统中心已收敛为“状态 → 音频 → 设置 → 在线更新”四页；`SETTINGS` 第五行明确标出
  配置、RTC、Wi-Fi 和本地 OTA 都由设置门户承载，旧独立维护页不再占用页面环。
- 用户确认 `0.11.0-dev.2` 的屏幕页面、实体按键、设置门户和既有功能均正常；本次验收后
  未再修改运行时代码、测试或固件配置。
- 设置门户的高风险边界仍以主机策略测试和双 OTA 回滚保护为主，没有通过故意断电、损坏
  NVS 或伪造固件在实机上进行破坏性测试。

### 正式发布构建

- 使用相同固定依赖将项目版本正式化为 `0.11.0`，重新完成仓库边界、许可材料、全部主机
  逻辑测试和 ESP-IDF v5.5.3 完整构建；最小应用分区仍有约 50% 空间。
- 正式 OTA 镜像大小为 1,588,144 bytes，SHA-256 为
  `31e6dc6c87c36798fa8fc77fe58db6314d51415d19a906eaa846513ad1eca6b2`；正式 Factory
  镜像大小为 1,653,680 bytes，SHA-256 为
  `dc71b66b9aeb1d56e68a742e170bfde8d91d56406caf804fe4d50d42e1be4d38`。
- 正式 OTA 与已实机验收的 `0.11.0-dev.2` OTA 大小一致，逐字节比较有 76 bytes 不同：
  应用描述中的版本、构建时间与 ELF 摘要，以及由这些元数据引起的镜像校验字节和末尾
  SHA-256；其余内容一致。
- 两类正式固件、校验文件和当前页面效果图保存到 `dist/v0.11.0/`。

## 2026-08-23：项目固件 v0.12.0 microSD 图片展示

### 候选构建与测试通道

- 固定依赖、仓库边界、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 完整构建通过；
  最小 3 MiB 应用分区仍有约 47% 空间。
- OTA 候选大小为 1,654,352 bytes，SHA-256 为
  `9e62e3feef4957f4c070d0bb571653a335d2c80f7e139843adce6c0ac69025ed`；Factory 合并
  镜像大小为 1,719,888 bytes，SHA-256 为
  `3ab35653471b0685ec9516ac8efecca5f1f22182187403e8ee7cc0f18bbccdd9`。
- OTA 候选发布到隔离的 `testing.json`，线上回读版本、大小和 SHA-256 与本地构建一致；
  `stable.json` 继续指向 v0.11.0。

### 无卡与存储卡验收

- 先在不插 microSD 的状态完成 Beta OTA：设备正常进入首屏，用户确认 `BOOT` 保持
  “首屏 → 月历 → 首屏”，没有启动错误或原有功能回退。
- 物理关机后插入出厂 FAT32 的 16 GB microSD。启动日志确认 `microSD mounted:
  14910 MiB`；卡内缺少 `/rlcd/images/` 时记录 `directory missing` 并隐藏图片页，首屏、
  RTC、联网与自动校时继续正常。
- 在 Windows 中只读核对该卡为健康的 15,634,268,160-byte USB 可移动磁盘、MBR 单分区
  和 FAT32；没有重新格式化。创建固定目录后只写入严格校验的
  `/rlcd/images/00-rlcd-test.pbm`，文件大小为 15,011 bytes，SHA-256 为
  `969c0a812d641f06a560748e91abe84bfb593bd69aac811b69be95072ec4eb26`，卡上回读一致。

### 图片与交互验收

- 再次物理关机插卡并正常开机后，`BOOT` 页面环条件扩展为“首屏 → 月历 → 图片 →
  首屏”；测试图中的方向、黑白极性和版面均正确。
- 固件覆盖的底部 50 像素黑色操作区、分隔线和 `BOOT: PAGE | KEY: SYSTEM` 清晰可见；
  用户确认显示和交互没有问题。
- 本次实机使用推荐的 PBM P4 正常路径，并覆盖无卡和目录缺失降级。受限 1-bit BMP、
  损坏图片、32 个候选上限及第 33 个候选拒绝由主机测试覆盖；非 FAT32、损坏文件系统
  和挂载失败尚未逐项实机制造。

### 正式发布构建

- 实机验收后使用同一固定依赖将项目版本正式化为 `0.12.0`，重新完成仓库边界、
  许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 完整构建；最小 3 MiB 应用分区
  仍有约 47% 空间。
- 正式 OTA 镜像大小为 1,654,352 bytes，SHA-256 为
  `9fa60050bceae2c12d093c03221ecf3b179a58a54ea00cbd662d2b75c96c6fad`；正式 Factory
  合并镜像大小为 1,719,888 bytes，SHA-256 为
  `737f1b2fec1bd45a95572744b61c7d102688812c4111935931b9733ff4110571`。
- 正式 OTA 与已完成实机验收的 `0.12.0-dev.1` OTA 大小一致，逐字节比较只有
  76 bytes 不同，全部位于应用描述中的版本、构建时间、ELF 摘要，以及由这些元数据
  引起的镜像校验字节和末尾 SHA-256；运行时代码与配置一致。
- 两类正式固件、校验文件和当前页面效果图保存到 `dist/v0.12.0/`。

## 2026-08-23：项目固件 v0.13.0 环境舒适度提示

### 候选构建与测试通道

- 固定依赖、仓库边界、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 完整构建通过；
  最小 3 MiB 应用分区仍有约 47% 空间。
- `0.13.0-dev.1` OTA 候选大小为 1,655,088 bytes，SHA-256 为
  `40ed73578d330f447a1520221460a5117dfd3a9759a77e57816037ae76db01f2`；Factory 合并
  镜像 SHA-256 为
  `73fe31a5bd1366efa95fde05ce81fc9a966b00f44c5f26ff12f7b89905592cc7`。
- OTA 候选发布到隔离的 `testing.json`，线上回读文件与本地构建逐字节一致；
  `stable.json` 继续指向 v0.12.0。

### 实机验收

- 设备通过开发者测试通道在线更新至 `0.13.0-dev.1`，首屏正常显示新增的环境舒适度
  表情，没有观察到布局异常。
- 用户确认当前约 31 °C、73% RH 时显示“需调整”皱脸，与两项取较差状态的判定规则
  一致，说明候选安装、传感器判定和屏幕绘制链路正常。
- 舒适与一般阈值、短时波动、读取失败、边界滞回和状态切换延迟由主机测试覆盖；本次
  实机没有使用环境箱逐项制造全部温湿度边界。
- 候选验收阶段没有提前生成 v0.13.0 正式发布文件、标签或切换稳定通道。

### 正式发布构建

- 实机验收后使用同一固定依赖将项目版本正式化为 `0.13.0`，重新完成许可材料、全部主机
  逻辑测试和 ESP-IDF v5.5.3 完整构建；最小 3 MiB 应用分区仍有约 47% 空间。
- 正式 OTA 镜像大小为 1,655,088 bytes，SHA-256 为
  `5505d813565b449fac58f21b30357630bfe31f0520bcdf432679ca65687a885e`；正式 Factory
  合并镜像大小为 1,720,624 bytes，SHA-256 为
  `ed6a0b5f946347ba18ccf3d86d7d31140ad22c76c99018b57e1fbd7c56bb025f`。
- 正式 OTA 与已完成实机验收的 `0.13.0-dev.1` OTA 大小一致，逐字节比较只有
  76 bytes 不同，全部位于应用描述中的版本、构建时间、ELF 摘要，以及由这些元数据
  引起的镜像校验字节和末尾 SHA-256；Factory 前缀及内嵌应用也通过一致性审计。
- 两类正式固件、校验文件和当前页面效果图保存到 `dist/v0.13.0/`。

## 2026-08-23：项目固件 v0.14.0 离线闹钟

### 候选构建与测试通道

- 候选源码提交为 `363fddd`；固定依赖、仓库边界、许可材料、全部主机逻辑测试和
  ESP-IDF v5.5.3 完整构建通过。
- `0.14.0-dev.1` OTA 候选大小为 1,666,128 bytes，SHA-256 为
  `34be8b533685b206ea2342f6d4e8e3448be06bcd0243c86e5e60f2d5e97c3d84`；Factory 合并
  镜像大小为 1,731,664 bytes，SHA-256 为
  `d2c1c413a9df5e5b8fa7e6e99e11bb5078a894c5d6d08f94c99c1d636d61eb05`。
- OTA 候选发布到隔离的 `testing.json`；验收期间 `stable.json` 继续指向 v0.13.0，
  没有提前向稳定通道发布候选。

### 实机验收

- 设备通过开发者测试通道在线更新至 `0.14.0-dev.1`，设置门户能够保存单个每周闹钟；
  到点提醒界面和三音提示正常显示、播放。
- 用户确认首次响铃短按 `KEY` 延后 5 分钟、延后再次响铃后短按 `BOOT` 停止均有效，
  本轮实机验收操作没有发现问题。
- RTC 无效、日期回拨、错过分钟不补发、60 秒上限、计时回绕、历史记录损坏以及
  `NORMAL/SAVING` 调度边界由主机测试覆盖；物理关机期间按设计不会响铃或补发。

### 正式发布构建

- 实机验收后使用同一固定依赖将项目版本正式化为 `0.14.0`，重新完成仓库边界、许可材料、
  全部主机逻辑测试和 ESP-IDF v5.5.3 完整构建；最小 3 MiB 应用分区仍有约 47% 空间。
- 正式 OTA 镜像大小为 1,666,128 bytes，SHA-256 为
  `a651ef871db69ea7bfc053de5433696d0076856167f127efa83df2bc41d68a84`；正式 Factory
  合并镜像大小为 1,731,664 bytes，SHA-256 为
  `0a5d2529bc295f8dec63900710a6f5a56dc46cedb6e4599ec18e4412e107f877`。
- 正式 OTA 与已完成实机验收的 `0.14.0-dev.1` OTA 大小一致，逐字节比较只有
  77 bytes 不同，全部位于应用描述中的版本、构建时间、ELF 摘要，以及由这些元数据
  引起的镜像校验字节和末尾 SHA-256；运行时代码与配置一致。Factory 的 64 KiB 前缀
  在候选与正式构建间一致，内嵌应用也分别与对应 OTA 镜像逐字节一致。
- 两类正式固件、校验文件和当前页面效果图保存到 `dist/v0.14.0/`。

## 2026-08-25：项目固件 v0.15.0 microSD 图片管理

### 候选构建与测试通道

- v0.15.0 候选逐步完成手机图片导入、公共演示图下载、运行时缓存更新、设置热应用和
  多图管理；所有候选只发布到隔离的 `testing.json`，验收期间 `stable.json` 始终保持
  v0.14.0。
- 最终实机候选 `0.15.0-dev.4` OTA 大小为 1,707,104 bytes，SHA-256 为
  `29cd39ae5e48b09eee5651c5683a901f3003028b376bf515d42e0d022ae104ff`；Factory 合并
  镜像大小为 1,772,640 bytes，SHA-256 为
  `4913fceae2b265c9ed08c94787c9d5434b0504195977804337be4649cdc5dcc9`。
- 线上回读的测试清单、OTA 大小和 SHA-256 与本地构建一致；测试固件未进入正式发布目录，
  也没有提前修改稳定通道。

### 实机验收

- 设备通过开发者测试通道在线更新到 `0.15.0-dev.4`，屏幕、实体按键、RTC、网络和已有
  页面正常；16 GB FAT32 microSD 继续作为可选内容源，无卡不影响其他本地功能。
- 手机浏览器完成 JPEG/PNG 本地转换、黑白预览和确认写入；设备也能在用户确认后使用
  已保存 Wi-Fi 下载并校验公共演示图。两条路径都直接更新运行时图片缓存，不重启设备。
- 多图图片页短按 `KEY` 能循环下一张，footer 位置正确；重启后恢复用户最后选择的图片，
  不按小时、日期或其他周期自动轮播。
- 设置门户能够逐张预览、前后浏览、设为当前并二次确认删除；普通设置、恢复默认值、
  手机导入、图片选择和删除保持设置热点与网页连接。清除 Wi-Fi 原地切换到配网，只有
  本地或在线 OTA 会按设计重启。
- 用户按最终验收清单操作后确认没有发现问题。数量超限、摘要冲突、损坏文件系统、传输
  截断、NVS 写入失败和存储卡卸载失败没有在实机逐项破坏性制造；能够纯逻辑化的输入、
  格式和事务策略由主机测试覆盖，其余场景不记录为已完成验证。

### 正式发布构建

- 使用相同固定依赖将项目版本正式化为 `0.15.0`，重新完成依赖、许可材料、全部主机测试
  和 ESP-IDF v5.5.3 完整构建；最小 3 MiB 应用分区仍有 46% 空间。
- 正式 OTA 镜像大小为 1,707,104 bytes，SHA-256 为
  `122f53c1c98bf09e84d7d5dbf957fef6086a44f50529d4c9e1274f651e875021`；正式 Factory
  合并镜像大小为 1,772,640 bytes，SHA-256 为
  `793c675729db808dcb5b12d565666adf6bc8a037983246d5bd30bbc0fbe7f3de`。
- 正式 OTA 与已完成实机验收的 `0.15.0-dev.4` OTA 大小一致，逐字节比较只有
  77 bytes 不同，全部位于应用描述中的版本、构建时间、ELF 摘要，以及由这些元数据
  引起的镜像校验字节和末尾 SHA-256；Factory 的 64 KiB 前缀一致，内嵌应用分别与
  对应 OTA 镜像逐字节一致。
- 两类正式固件、校验文件和当前页面效果图保存到 `dist/v0.15.0/`。

## 2026-08-26：项目固件 v0.16.0 图片页安全删除

### 候选构建与测试通道

- `0.16.0-dev.1` OTA 候选大小为 1,711,984 bytes，SHA-256 为
  `481d914a9f1ee819a4891f0139cccca7343e839c4eb066ec1a9717afe306a4da`；镜像内嵌项目名、
  版本、ESP32-S3 类型、校验和与 Validation Hash 均通过检查，能够放入 v0.15.0 已有的
  3 MiB OTA 应用槽；
- 使用站点仓库固定的 `deploy-testing-channel.sh` 流程，将候选发布到隔离的
  `testing/v0.16.0-dev.1/` 不可变路径；公网回读文件与本地构建逐字节一致，版本化文件
  使用长期缓存，`testing.json` 不缓存并已原子切换到该候选；
- `stable.json` 继续指向 v0.15.0。只有主动开启 Beta 更新的设备会发现候选；该候选尚未
  进入正式发布目录、Git 标签或稳定通道，后续修复必须使用新的 `dev.N` 版本，不能覆盖
  已发布的 `dev.1` 文件。

### 实机验收

- 设备从 v0.15.0 通过开发者测试通道发现并安装 `0.16.0-dev.1`，下载、校验和重启完成；
  用户确认在线升级与本版功能未见问题，作为 OTA 和图片删除正常路径的整体冒烟验收；
- 长按阈值、松开门控、二次确认、`BOOT`/超时取消、目标变化、结果计时与删除后选择策略
  由主机测试覆盖。慢卡、只读卡、强制写入失败和写卡期间触发闹钟未逐项进行破坏性实机
  验证；SDMMC、真实介质联动及闹钟抢占不在集成主机测试覆盖范围。

### 正式发布构建

- 使用相同固定依赖将项目版本正式化为 `0.16.0`，重新完成依赖、许可材料、全部主机测试
  和 ESP-IDF v5.5.3 完整构建；最小 3 MiB 应用分区仍有 46% 空间；
- 正式 OTA 镜像大小为 1,711,984 bytes，SHA-256 为
  `a3f3744520592841d61c0bbacacb1f05f94adac9b10687930dca57740fc74671`；正式 Factory
  合并镜像大小为 1,777,520 bytes，SHA-256 为
  `6322779fda57bccf6d26196c8b02cd891cbf8779b97f75598539944dd0abd4b9`；
- 正式 OTA 与已验收候选大小一致，逐字节比较只有 76 bytes 不同，全部位于应用描述中的
  版本、构建时间、ELF 摘要和末尾镜像 SHA-256；运行时代码与配置一致。Factory 的
  64 KiB 前缀一致，内嵌应用分别与对应 OTA 镜像逐字节一致；
- 两类正式固件、校验文件和当前页面效果图保存到 `dist/v0.16.0/`。

## 2026-08-27：项目固件 v0.18.0 自动低功耗与离线语音

### 版本整合

- 原计划中的 v0.17.0 没有单独正式发布；自动低功耗、设备端手动提前省电和 Wi-Fi 当前
  连接图标与首版离线语音一并进入 v0.18.0。
- 自动规则始终生效，不需要用户选择“自动模式”：低电时进入 `SAVING` 并隐藏秒数，
  达到恢复条件或检测到电脑 USB 数据主机后回到 `NORMAL`；设备端手动开关只用于提前
  省电，不能关闭自动低电保护。

### 低功耗实机观察

- 用户确认 `SAVING` 页面、秒数隐藏、Wi-Fi 图标随当前连接状态显示以及设备端操作均正常。
- 同一设备的端电压估算曾在拔线后从 20% 下降至 18%，插线后立即升至 44% 并继续到 48%，
  再次拔线后从 48% 回落到 22% 和 19%。该序列用于确认阈值与页面行为，不代表电池在数秒
  内充入或失去相应容量。
- 本轮没有使用外部电流表或功耗分析仪测量稳态电流、峰值和续航，也没有把普通充电器、
  充电宝或纯充电线记录为可由 ESP32 可靠识别的外部电源。

### app+model USB 更新

- 首版语音使用 ESP-SR 2.4.7 中文 MultiNet5；模型区域固定为 `0x610000` 起的 3 MiB，
  原有两个 3 MiB OTA 应用槽保持不变。
- 设备进入 ROM 下载模式后，通过固定的 `update-app.sh` 流程在同一次 `write-flash` 中把
  `0.18.0-dev.1` 应用写入 `0x10000`、模型写入 `0x610000`，随后只清除 OTA 选择数据。
  esptool 完成 Flash 哈希校验；旧分区表、NVS、家庭 Wi-Fi 与设备偏好均保留。
- 正常开机后屏幕与原有页面显示正常，已保存 Wi-Fi 继续可用；模型完整性校验和离线识别
  引擎正常完成，`VOICE` 页面进入就绪状态。

### 离线语音实机验收

- 用户在“语音”页按住 `KEY` 2 秒，看到松开提示后开始一次短会话；“打开日历”能够正确
  识别并进入月历页。
- 随后确认“查看状态”和“回到主页”均能进入对应页面；活动会话短按 `BOOT` 显示取消，
  不执行迟到的旧结果。用户确认屏幕与上述交互均无异常。
- 本轮没有把“打开图片”“打开设置”、无图片目标、未知指令、远距离与多噪声识别、闹钟
  在各阶段抢占、20—50 次连续会话内存趋势或低电语音峰值记录为已完成实机验证；对应
  白名单、会话门控、目标缺失、会话编号和迟到结果边界由主机测试覆盖。

### 正式发布构建

- 正式 OTA 镜像大小为 1,927,968 bytes，SHA-256 为
  `67cfeae09efc5bb078e6a806870f9a971fb5c41b8722a7f1cf4ac60fcca3a2e5`；模型大小为
  2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`。
- 包含 bootloader、分区表、应用和同一模型的正式 Factory 镜像大小为 8,560,811 bytes，
  SHA-256 为
  `121615af71d3fe3ef09e3be03c9e2d8cba8ef4eb4029419026e16f4ae7859489`；构建流程逐字节核对
  Factory 在 `0x610000` 的模型片段与独立模型一致。
- 正式 OTA 与已实机验收的 `0.18.0-dev.1` 长度相同，逐字节比较只有 74 bytes 不同，
  差异位于应用版本、构建元数据、ELF 摘要和镜像摘要；模型逐字节一致。
- 本轮没有在目标板上另行执行 Factory 完整重装或纯应用 OTA 的缺模型降级路径；Factory
  内容和缺模型安全退化由构建检查与主机逻辑覆盖，不写成已完成实机验证。

## 2026-08-29：项目固件 v0.19.0 日常联网与环境显示可靠性

### 候选与网络行为

- 本版将设置门户的 Wi-Fi 管理改为“显示已保存网络、在内存中验证候选、取得 IPv4 后再
  保存”。验证失败保留旧凭据和门户，成功后关闭门户并按实际电源状态恢复网络，全程不
  重启设备；密码不回传、不预填也不写入日志。
- `NORMAL` 取得家庭网络 IPv4 后保持 STA 连接，并启用 `WIFI_PS_MIN_MODEM`；首屏图标
  只表达当前已连接，状态页把当前链路和最近时间同步分开显示。`SAVING` 仍只在用户主动
  校时、更新或下载图库时临时联网。
- 用户安装 `0.19.0-dev.1` 后确认首屏、Wi-Fi 和原有功能未见异常；当前连接的 Wi-Fi
  图标能够正常显示。固定 50 次语音矩阵尚未执行，但用户确认普通离线识别继续正常。

### 温湿度稳定性

- `0.19.0-dev.2` 为 SHTC3 每次公开读取增加至多一次受控恢复重试；两段 CRC 已通过时，
  即使后续休眠命令失败也保留有效测量，并在下一次读取前恢复传感器状态。
- 首个有效样本立即显示三态表情。一次完整读取失败后首屏继续显示最近有效温湿度和表情，
  状态页标记 `STALE`；从最近成功样本起满 180 秒仍未恢复才显示占位符。日常显示使用近
  一分钟有效样本中位数，并按屏幕实际精度决定是否刷新。
- 候选 OTA 大小为 1,945,696 bytes，SHA-256 为
  `723fe8e3ff3beb622aff2886753491461006fe69e17d0b7cd9a3a025f80444ac`；通过固定测试通道
  发布后，线上文件大小和摘要与本地构建一致。用户完成在线升级并确认温湿度与表情日常
  显示稳定，其他功能未见问题。
- 本轮没有人为断开传感器或制造连续 I2C 错误；恢复重试、179999 / 180000 毫秒边界、
  tick 回绕和后续成功恢复由纯逻辑主机测试及目标构建覆盖，不把这些结果写成已完成硬件
  故障注入。

### 正式发布构建

- 使用相同固定依赖显式构建 `0.19.0`；应用描述版本正确，正式 OTA 大小为
  1,945,696 bytes，SHA-256 为
  `023b0be722f577724094fe77cc0e64ccabd190d4e4752e8adecbee15d61aa4ce`，最小 3 MiB 应用
  分区仍有 38% 空间。
- 模型大小为 2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；包含 bootloader、
  分区表、应用和同一模型的 Factory 镜像大小为 8,560,811 bytes，SHA-256 为
  `e682ed89373a02c8935a86dd5de47a9d65df7eba310571902a21cdee93f2c8a4`。
- 正式 OTA 与已验收 `0.19.0-dev.2` 长度相同，逐字节比较只有 75 bytes 不同，均属于
  版本、构建元数据、ELF 摘要和镜像摘要；模型内容与 v0.18.0 及本版构建清单逐字节一致。
- 三项正式二进制、三条校验值、版本说明和当前效果图保存到 `dist/v0.19.0/`。本轮不重复
  执行 Factory 完整重装、旧设备补装模型、Wi-Fi 错误密码换网、外部仪表功耗或连续语音
  统计测试；这些边界不作为本版本实机结论。

## 2026-08-30：项目固件 v0.20.0 云端语音 Beta

### 候选与实机结果

- 本版在原“语音”页加入可选的阿里云百炼 Realtime 单轮对话；用户提供自己的 API Key，
  默认使用北京共享域名和 `qwen3-omni-flash-realtime`。未开启、未配置、家庭 Wi-Fi
  离线或设备处于 `SAVING` 时仍直接使用本地 MultiNet，不增加常驻监听。
- `0.20.0-dev.3` OTA 大小为 2,001,120 bytes，SHA-256 为
  `df03fa825b849201b7fc97dbbbbe054acea957f05f9ec90bdf66aa50f018b232`。通过固定
  `update-app.sh` 流程写入应用与同版本模型，保留原有 NVS 配置。
- 用户确认首屏和原有功能正常；手机设置门户的普通文本输入框能够直接粘贴较长 API Key，
  保存后可用默认模型完成单轮语音对话，屏幕文字和扬声器回复正常。当前每次长按是独立
  单轮，不跨会话保留上下文。
- 本轮没有把可选 Qwen Audio 3 模型、错误或无权限 Key、新加坡及 Workspace 专属 Host、
  接近 10 秒输入、各阶段取消、云端中途断网、闹钟抢占或长时间连续会话写成已完成实机
  验证；配置白名单、协议解析、失败清理和后端选择边界由主机测试覆盖。

### 正式发布构建

- 使用相同固定依赖显式构建 `0.20.0`；正式 OTA 大小为 2,001,120 bytes，SHA-256 为
  `b2a51f112056a896452f814f5f543e35cd67b2974285515c36c78058e41c5e35`，3 MiB 应用
  分区仍有约 36% 空间。
- 模型大小为 2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；包含 bootloader、
  分区表、应用和同一模型的 Factory 镜像大小为 8,560,811 bytes，SHA-256 为
  `bc97614f3b4235d426b122d6bd4c2d683db4f7b098d5a68660eb4291da17cb2f`。
- 正式 OTA 与已验收 `0.20.0-dev.3` 长度相同，逐字节比较只有 73 bytes 不同，均属于
  版本、构建元数据、ELF 摘要和镜像摘要；Factory 内 `0x610000` 的模型片段与独立
  `-model.bin` 逐字节一致。

## 2026-08-31：项目固件 v0.21.0 有界多轮 AI 语音

### 候选与实机结果

- 本版在 v0.20.0 单轮直连的基础上，让一次按键发起的 AI 语音会话复用同一 TLS
  WebSocket 和服务端上下文；最多 5 轮，每轮回复后等待续问最长 30 秒。交互仍为用户
  主动发起的手动 PTT 半双工，不加入唤醒词、后台麦克风或跨会话历史。
- 最终实机候选 `0.21.0-dev.2` OTA 大小为 2,005,600 bytes，SHA-256 为
  `3272a11eb4a33fde4d58f12dd22063cf3222f89f3219cf5b4da6969552a61b1b`。
- 用户在目标板上用默认模型先给出数字、再在下一轮追问，确认服务端上下文能够延续；
  播放中短按 `KEY` 能显示 `NEXT TURN` 并进入续问，回复完成后的 `FOLLOW-UP` 可用短按
  `KEY` 继续、短按 `BOOT` 结束。首屏和既有功能未见异常。
- 用户随后发现 `0.21.0-dev.2` 的 90 秒硬截止从联网前开始累计，并在第三轮正常播放中
  直接结束会话；回答文字也固定停在最前两行并追加省略号。该候选不再作为正式发布依据。
- 修复候选 `0.21.0-dev.3` 将总边界改为 5 分钟下一轮准入期限；已经开始的收音和回答
  不会被总时限截断。回答改为按真实字体宽度换行、持续显示最新 4 行，超过 513 bytes
  时保留最新的完整 UTF-8 文本。
- `0.21.0-dev.3` OTA 大小为 2,006,176 bytes，SHA-256 为
  `314ee11c1627f0de7d37f308f2a603360f1c2e33ee61b7c7e1558b7afb29c600`。用户在目标板确认
  对话能够越过原第三轮截止点，回答可以完整播放；屏幕会随流式回复持续显示最新 4 行、
  覆盖最旧行且不再追加省略号。第 5 轮、30 秒续问、5 分钟准入边界、资源回收、断网、
  鉴权失败和闹钟抢占仍未记录为本轮目标板专项结果；相应边界和显示逻辑已有主机测试。

### 正式发布构建

- 使用同一冻结源码和固定依赖显式构建 `0.21.0`；正式 OTA 大小为 2,006,176 bytes，
  SHA-256 为 `82db5380186ff9bf5c327b20af23623f827d4cf134e18dfc07844d3b53c9bfbb`，
  3 MiB 应用分区仍有约 36% 空间。
- 模型大小为 2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；包含 bootloader、
  分区表、应用和同一模型的 Factory 镜像大小为 8,560,811 bytes，SHA-256 为
  `e47a6956addcd8d27e5f52e7bf99a8b9f24d3661d4fb07041c6b1435be324461`。
- 正式 OTA 与已验收 `0.21.0-dev.3` 长度相同，逐字节比较只有 73 bytes 不同，均位于
  版本、构建时间、ELF 摘要和镜像摘要元数据；Factory 在 `0x610000` 内嵌的模型与独立
  `-model.bin` 逐字节一致。

## 2026-09-01：v0.22.0-dev.1 对话体验候选

- 系统中心用户页面由 `VOICE` 改为 `CHAT`，并用 `AI CHAT` 与 `OFFLINE COMMANDS`
  区分云端对话和本地指令；内部音频与诊断标识不做无意义的批量改名。
- 默认回答提示改为一至三个短句，Qwen3 Omni 设置 256 Token 输出上限。等待逻辑不再把
  整段播放限制在 45 秒内，只检测首次无音频和中途无进展；输出达到上限时继续排空已有
  PCM，不再在播完后误报失败。
- 回复文字改为按本地已播放 PCM 逐步显示，并处理完整转写到达时的字幕回退、音频生产与
  播放交错、静音快速排空和只有文字没有 PCM 的降级显示。
- 主机测试、仓库检查、许可检查和 ESP-IDF v5.5.3 完整构建通过。候选 OTA 大小为
  2,008,352 bytes，SHA-256 为
  `1280fcd11bdcbb3541cea0ae37a864d7b6e2602e5bdbe5c78a5ed9877d72c58d`，3 MiB 应用分区
  剩余约 36%，已经上传到开发者测试通道。
- 用户通过开发者测试通道安装候选后，确认页面命名、回答长度、字幕随声音推进和原有多轮
  操作正常，并同意发布正式版。静音字幕节流、长度上限截断、第五轮边界、断网、鉴权失败
  和闹钟抢占未在本轮分别记录为专项实机结果，不据此扩大验收结论。

### 正式发布构建

- 使用同一冻结源码和固定依赖显式构建 `0.22.0`；正式 OTA 大小为 2,008,352 bytes，
  SHA-256 为 `1911c1e3170fb2475bf225a08234cf1f603a62012d95c8f6a1d1f316eb220c73`，
  3 MiB 应用分区仍有约 36% 空间。
- 模型大小为 2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；包含 bootloader、
  分区表、应用和同一模型的 Factory 镜像大小为 8,560,811 bytes，SHA-256 为
  `1c438d0582f0876921ceeee17d1a2191e45e8b92788041e856bcb7974bcb2753`。
- 正式 OTA 与已验收 `0.22.0-dev.1` 长度相同，逐字节比较只有 74 bytes 不同：版本字段
  6 bytes、构建时间 4 bytes、ELF 摘要 32 bytes 和镜像摘要 32 bytes；段布局和可执行
  内容没有其他差异。Factory 在 `0x610000` 内嵌的模型与独立 `-model.bin` 逐字节一致。

## 2026-09-01：项目固件 v0.23.0 交互改进

- 本轮将稳态页脚改为直接说明目的地或动作，并为有效长按加入统一的进度与松开取消提示；
  `AI CHAT` 与 `OFFLINE COMMANDS` 的空闲提示也分别说明提问和设备指令目标。
- 用户在目标板安装本轮 OTA 候选，确认整体交互改进符合预期并授权发布 v0.23.0。
- 本轮确认范围是整体交互结果，没有形成逐项专项实机矩阵，不据此扩大验收结论。

### 正式发布构建

- 使用同一冻结源码和固定依赖显式构建 `0.23.0`；正式 OTA 大小为 2,009,056 bytes，
  SHA-256 为 `12fa7f8c214106581584870698522aa32b5c4d68b6e67c17d7e6b648c4a61f71`，
  3 MiB 应用分区仍有约 36% 空间。
- 模型大小为 2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；包含 Bootloader、
  分区表、应用和同一模型的 Factory 镜像大小为 8,560,811 bytes，SHA-256 为
  `c29551af417b497520b8136638d6daf51af8c6a3be1bd43f3220ff28f495397b`。
- 正式 OTA 与候选长度相同，逐字节比较共有 75 bytes 不同：版本字段 6 bytes、构建时间
  4 bytes、ELF 摘要 32 bytes，以及镜像校验字节和镜像摘要 33 bytes；功能代码没有
  其他差异。Factory 在 `0x610000` 内嵌的模型与独立 `-model.bin` 逐字节一致。

## 2026-09-02：v0.24.0 天气 Beta

- 设置门户完成 QWeather API Host、API Key 与“省份 → 城市”配置后，设备能够直接解析
  地点并获取实时天气与三日预报；保存配置不重启，天气页在固定布局内显示获取状态和结果。
- 初始候选在收到 QWeather Gzip 响应时发生重启。静态检查确认 miniz 高层解压函数会在
  12 KiB 天气任务栈上创建约 11 KiB 解压状态，叠加调用链后触发 FreeRTOS 栈保护。
  `0.24.0-dev.6` 改用低层解压接口并将状态放入堆或 PSRAM，同时把任务栈增至 16 KiB；
  用户通过在线更新恢复后确认真实天气正常显示且不再重启。
- 主机测试覆盖天气配置、NVS 记录、地点目录、请求构造、JSON 响应、Gzip 帧校验、缓存
  记录和页面模型；仓库、许可与固定依赖检查通过。天气仍为 Beta，本轮不把未逐项验证的
  断网、错误凭据、缓存跨重启、`SAVING` 收尾或长期运行扩大表述为实机结果。

### 正式发布构建

- 使用同一源码和固定依赖显式构建 `0.24.0`；正式 OTA 大小为 2,072,304 bytes，
  SHA-256 为 `dd5bfc8d38f6a05f5f34d776bc474bbe766459ca7fe12f376ab193959569e80b`，
  3 MiB 应用分区仍有 34% 空间。
- 模型大小为 2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；包含 Bootloader、
  分区表、应用和同一模型的 Factory 镜像大小为 8,560,811 bytes，SHA-256 为
  `2e95b53e97b06ec993e0c621ad370026fe96ce5774f50aa38ad9dada5be7314a`。

## 2026-09-05：v0.25.0-dev.1 启动恢复候选

- 增加同一应用镜像连续异常启动后的恢复模式，以及开机按住 `KEY` 的手动恢复入口。
  恢复模式保留在线更新和精简设置门户；无 Wi-Fi 配置时不自动开启普通配网热点，按键
  初始化失败时等待网络就绪并自动重试开放门户。旧 Wi-Fi 配置不可读也不阻断本地 OTA。
- OTA 确认等待 15 秒和核心启动里程碑；60 秒后清除启动异常计数。计划重启标记与健康
  标记交错执行时仍保留一次性重启意图。天气页按住 `KEY` 2 秒可手动刷新，并增加本地
  Gzip 解码自检、天气任务资源日志和 `GET_BOOT` 诊断。
- 主机测试、仓库与许可检查以及固定 ESP-IDF v5.5.3 构建通过。真实 RTC 记录适配代码在
  主机桩下验证了复位分类、连续故障、记录损坏、镜像切换和计划重启；浏览器脚本检查覆盖
  已配网、未配网和旧配置不可读三种恢复门户的 OTA 令牌流程。两张恢复页 SVG 已检查排版。
- 使用 `RLCD_PROJECT_VERSION=0.25.0-dev.1 ./scripts/build.sh` 生成候选。OTA 为
  2,086,768 bytes，SHA-256 为
  `69f01e7b9021c290f1ac7995ba9f0ef28a0f4244e6e5a76fe1ae40d990fb438a`；3 MiB 应用槽
  剩余约 34%。Factory 为 8,560,811 bytes，SHA-256 为
  `e524bcb6a8f932f2ded914962d914e70ba4e390ceb5b22c54075c07109ca6dcf`。模型内容及
  `0x610000` 偏移保持不变，构建产物全部通过 `SHA256SUMS` 校验。
- 通过站点仓库的 `scripts/deploy-testing-channel.sh` 上传候选，最低 OTA 版本为
  `0.7.0`，发布日期为 `2026-09-05`。本地与远端清单校验通过，公网下载与本地 OTA 逐字节
  一致；`testing.json` 返回禁止缓存，版本化镜像使用长期缓存。稳定清单部署前后完全一致，
  仍指向 `0.24.0`。上个测试清单保存在本项目备份目录
  `testing-20260905T021541Z-d06251062ef8`。
- 本轮未向目标板写入固件。正常启动、天气刷新、手动恢复、真实异常复位与恢复 OTA 等
  实机项目仍按[开发与发布流程](development.md#v0250-启动恢复候选验收)等待验收，未记录
  为通过；候选没有加入正式 `dist/`，也没有创建正式 GitHub Release。

## 2026-09-05：v0.25.0-dev.2 天气日期布局

- 天气顶栏改为城市与 RTC 当前月日、星期，QWeather 数据获取时间移到底部并带上月日；
  缓存过期、刷新中或刷新失败时继续保留已有数据时间。RTC 无效时日期使用占位符，不从
  缓存推断今天。实况、三日预报的尺寸与位置保持不变，没有改动天气请求、解压和启动流程。
- 主机测试、仓库与许可检查以及固定 ESP-IDF v5.5.3 构建通过。日期测试覆盖一周七天、
  跨年、闰年与世纪年、无效日期、缓冲区边界，以及 RTC 已跨天而缓存仍是昨天的情况。
  使用固件同一份 u8g2 字体检查宽度：`12月31日  周四` 为 113 px，长城市名
  `克孜勒苏柯尔克孜自治州` 为 174 px，两者加 12 px 间隔不超过 376 px 内容区；最长
  来源示例含“已过期”和“更新中”时为 290 px，按键提示为 305 px。SVG 效果图已更新并
  在本地检查排版；这些检查不代替实机验收。
- 使用 `RLCD_PROJECT_VERSION=0.25.0-dev.2 ./scripts/build.sh` 生成候选。OTA 为
  2,087,264 bytes，SHA-256 为
  `4324190870de3ae5bb43e184274d6a8f77f3460018bb239251ed8ac01ba39180`；3 MiB 应用槽
  剩余约 34%。Factory 为 8,560,811 bytes，SHA-256 为
  `294452cd881cb3f139067bfada95b10dabb8ef23175ebcc71120e23055f39fc2`。模型及分区表不变，
  构建产物全部通过 `SHA256SUMS` 校验。
- 沿用站点仓库 `scripts/deploy-testing-channel.sh` 部署到独立测试版本目录，最低 OTA
  版本仍为 `0.7.0`。公网清单与 OTA 均与本地逐字节一致；清单禁止缓存，版本镜像使用
  长期不可变缓存。稳定清单部署前后完全一致，仍为 `0.24.0`。旧测试清单备份为
  `testing-20260905T024028Z-d06251062ef8`。
- 本轮未向目标板写入固件，日期跨天、断网缓存和 RTC 无效时的实际观感仍待用户验证。
  不改正式 `dist/` 或 GitHub Release；恢复模式的专项实机验收要求仍保留。

## 2026-09-05：v0.25.0 正式发布

- 用户在天气日期候选完成后反馈“没啥问题”，并明确授权正式发布。本轮确认范围为候选
  天气界面的整体显示效果，没有新增真实异常复位注入、恢复门户 OTA 或回滚的专项记录；
  这些待办继续保留在[开发计划](roadmap.md)，不扩大实机验收结论。
- 冻结 `0.25.0-dev.2` 的功能源码，仅修改正式版本号、文档和发布材料。固定依赖检查、
  全部主机测试、恢复门户 JavaScript 检查和 ESP-IDF v5.5.3 正式构建通过。OTA 大小为
  2,087,264 bytes，SHA-256 为
  `2fc9eff71036077b905473db308744af043ef6c214b5077cb8180416a7943e52`，3 MiB 应用槽仍有
  34% 空间；Factory 大小为 8,560,811 bytes，SHA-256 为
  `3a38daf322f848b4f8b328c641131f15ff628709aa36a2332953dfb1bf3075d3`。
- 正式 OTA 与 `0.25.0-dev.2` 长度相同，逐字节比较只有 75 bytes 不同：版本字段 6 bytes、
  构建时间 5 bytes、ELF 摘要 32 bytes 和镜像校验字节及摘要 32 bytes；除此之外没有差异。
  语音模型仍为 2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`，Factory 在
  `0x610000` 内嵌的模型与独立附件逐字节一致。
- `dist/v0.25.0/` 保存同次构建的 Factory、OTA、模型、SHA256SUMS、版本说明和效果图；
  发布包同步收录天气新布局和两张恢复页示意，历史 `dist/` 文件保持原样。

## 2026-09-05：v0.26.0-dev.1 常驻显示与快捷设置候选

- 用户同意将下一轮收敛为“常驻显示 + 设备端快捷设置”。新增 `CLOCK`、`WEATHER`、
  `IMAGE` 常驻选择，保存后立即展示，正常启动与普通页面 30 秒无操作后回到所选页；
  条件不满足时使用时钟，但保留偏好。普通页面顺序、四个系统页与 `BOOT: HOME` 语义不变。
- 正常 `SETTINGS` 长按 `KEY` 3 秒打开四项快捷菜单，音量、已有闹钟开关与常驻页可
  直接编辑和保存，第四项保留 `WEB SETTINGS`。原 `BOOT` 长按手动省电与恢复模式
  直接打开精简门户的操作不变。操作与设计规则已写入[设备设置](device-settings.md)和
  [设计规范](design-guidelines.md)。
- 设置 schema 升为 v7，在独立新双槽中保存常驻偏好，v1—v6 迁移保留原有字段；
  设备修改只更新加锁后的最新记录中的单个字段，相同值不重复写入。取消、长按松开、
  30 秒编辑超时、闹钟抢占、保存失败与待应用反馈均有明确处理，不为普通设置重启或联网。
- 全部主机测试、恢复门户 JavaScript 检查、仓库与许可检查、固定依赖检查以及
  ESP-IDF v5.5.3 构建通过。新增真实设置服务的模拟 NVS 提交失败与重启测试、菜单与
  按键组合测试，以及真实 U8g2 字体宽度测试；CI 增加固定字体目录准备，不下载工具链。
  最长设置页脚为 365 px，快捷设置标题为 279 px，均在 376 px 安全区内；SVG 已检查。
- 使用 `RLCD_PROJECT_VERSION=0.26.0-dev.1 ./scripts/build.sh` 构建，OTA 为
  2,091,952 bytes（2.09 MB），SHA-256 为
  `1817a122d0987a49ae2148dc42d43425ff68fb5fa7c33cf30e5296cbe76fb1b4`；Factory 为
  8,560,811 bytes，SHA-256 为
  `7844b49caa24579805781fd669a8ec9223059fbd18224bb109e74e939bad7f6d`。分区与语音模型
  不变；3 MiB 应用槽余量约 33%。本地日志为 `build/v0.26.0-dev.1-build.log` 与
  `build/v0.26.0-dev.1-host-tests.log`，不提交构建目录。
- 沿用站点 `scripts/deploy-testing-channel.sh` 顺序部署，测试清单与公网 OTA 均与本地
  逐字节一致，清单禁止缓存，版本镜像长期不可变。稳定清单部署前后完全一致，仍为
  `0.25.0`，没有改动正式站点、Release、`dist/`、Docker 或其他服务。旧测试清单备份为
  `testing-20260905T042747Z-b59857d30994`。
- 本轮未给目标板写入固件，也未宣称实机通过。常驻页停留与重启、音量和闹钟热应用、
  实体按键与恢复模式仍需用户按[验收矩阵](development.md#v0260-页面停留与快捷设置验收)
  确认；模拟 NVS 故障不替代真实 Flash 断电、异常复位与 OTA 回滚验证。

## 2026-09-05：v0.26.0-dev.2 取消页面自动返回

- 用户明确选择“切到哪页就停在哪页，手动返回”，不需要另设常驻页。移除页面模型的
  默认目标、无操作计时器和启动图片跳转，日常页、系统页及快捷菜单列表均不自动返回。
  正常开机仍进入时钟；后台发现图片不会切走当前页面，图片仍保存选中的文件。
- 快捷菜单收敛为 `VOLUME`、`ALARM`、`WEB SETTINGS` 三项，设置摘要恢复温度单位。
  编辑中无操作 30 秒只取消草稿并返回菜单原选中项；闹钟抢占关闭菜单并丢弃草稿。
  对话、热点、删除确认与 OTA 确认的时限和输入保护不变，设计规范、用户文档与 SVG
  预览同步更新。
- 从运行模型和设置接口移除 DISPLAY，但继续使用 schema v7 和原双槽。dev.1 记录的
  字节 25 接受旧值 0—2 后忽略，之后保存时写 0，不因升级清除此项而在启动时写入。
  模拟 NVS 测试覆盖三种旧选择的完整偏好保留、启动零写入、单字段保存后再启动，以及
  schema v6 迁移中断与重试；该模拟不代表真实 Flash 断电测试。
- 全部主机测试、20 项 Python 测试、真实 U8g2 字体布局、恢复门户脚本、仓库与许可检查
  通过。使用现有 ESP-IDF v5.5.3 执行
  `RLCD_PROJECT_VERSION=0.26.0-dev.2 ./scripts/build.sh`，没有下载新工具链。
  OTA 为 2,090,096 bytes（2.09 MB），SHA-256 为
  `539c23b6cf643676d6fe7db34f8f26c6ed27cc10aa784f16feb3337546d4e995`；Factory 为
  8,560,811 bytes，SHA-256 为
  `a4fb55af319a30bbc7572d58e37dd7b99e76c1ecca06ccc1b17624a50c34d145`。
  应用槽剩余约 34%，分区与语音模型不变；日志保存在
  `build/v0.26.0-dev.2-build.log` 和 `build/v0.26.0-dev.2-host-tests.log`。
- 沿用站点固定 `scripts/deploy-testing-channel.sh` 顺序部署，只新增不可变
  `testing/v0.26.0-dev.2/` 并更新 `testing.json`，最低 OTA 版本为 `0.7.0`。公网 OTA 与
  清单均同本地产物逐字节一致，清单禁止缓存、镜像长期缓存；部署前后 `stable.json`
  逐字节相同，正式版仍为 `0.25.0`。旧测试指针备份为
  `/www/backup/mcu.taifua.com/testing-20260905T045719Z-ac42216bdff4`。
- 未向目标板写入或触发重启，尚待用户验证页面停留、手动返回、快捷菜单和编辑超时。
  本轮未修改正式网站、`dist/` 或 GitHub Release，也未操作 Docker、Nginx 或其他服务。
