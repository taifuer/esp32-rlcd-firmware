# Changelog

本项目的显著变化记录在此。版本号采用语义化版本格式。

## [Unreleased]

## [0.20.0] - 2026-08-30

### Added

- 新增可选的阿里云百炼 Realtime 云端短对话 Beta：用户只需开启功能并保存自己的 API
  Key，无需创建百炼应用或填写 Workspace ID、App ID；默认使用北京共享域名，高级设置
  可选填受控的阿里云官方 API Host。停用时保留 Key，设置保存后立即用于下一次会话，
  无需重启。
- “语音”页在云端可用时支持一次最长 10 秒的中英文提问，流式显示识别文本和回答并通过
  扬声器播放。默认模型为 `qwen3-omni-flash-realtime`，高级设置可选
  `qwen-audio-3.0-realtime-flash`；首版不启用多轮上下文、自动 VAD、Function Calling
  或云端设备控制。

### Changed

- 语音后端按每次按键会话锁定：仅在云端已开启、API Key 已保存、设备为 `NORMAL` 且家庭
  Wi-Fi 当前已连接时使用云端；停用、离线、省电或未配置时继续使用本地 MultiNet5 指令
  识别。云端回答只显示和播放，不会进入设备指令执行路径。
- 云端直连固定 `wss://`、Realtime 路径与受控模型参数；高级设置只接受北京或新加坡的
  百炼共享/业务空间专属 API Host，不接受完整 URL、路径、端口、IP 或任意域名。上行固定
  使用 16 kHz、16-bit 单声道 PCM，下行使用 24 kHz、16-bit 单声道 PCM。用户取消、
  闹钟抢占、超时和网络失败均有有界清理路径。

### Security

- 设置状态、网页预填和项目主动日志均不返回完整 API Key；空白保存保留原 Key，清除操作
  独立确认。严格的官方 API Host 白名单避免设备将 Key 发送到任意用户输入的主机。
- v0.20.0 的默认构建未启用 Flash / NVS 加密，因此拥有设备并读取 Flash 的人
  仍可能恢复 API Key。设置页和文档明确这一边界，并建议使用项目专用且可轮换的 Key；
  麦克风音频、模型标识与协议元数据只在用户主动发起云端会话后上传，识别文本和回复由
  服务端返回并可能产生费用。

### Development

- 固定 ESP WebSocket Client v1.8.0 的源码提交与许可检查；新增启用状态、API Key、受控
  模型与官方 API Host 配置双槽记录、表单解析、Realtime 协议事件、UTF-8 安全截断、
  语音后端选择和会话失败路径的主机测试。

### Verified

- 用户通过 USB 安装 `0.20.0-dev.3` 后确认首屏和原有功能正常；手机设置门户可直接粘贴
  较长 API Key、保存配置，并使用默认 `qwen3-omni-flash-realtime` 完成单轮语音对话，
  屏幕文字和扬声器回复正常。
- 正式 OTA 为 2,001,120 bytes，SHA-256 为
  `b2a51f112056a896452f814f5f543e35cd67b2974285515c36c78058e41c5e35`；模型为
  2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；Factory 为
  8,560,811 bytes，SHA-256 为
  `bc97614f3b4235d426b122d6bd4c2d683db4f7b098d5a68660eb4291da17cb2f`。
- 正式 OTA 与已实机验收的 `0.20.0-dev.3` 长度相同，仅 73 bytes 的版本、构建元数据、
  ELF 摘要和镜像摘要不同；模型逐字节一致。
- 可选 Qwen Audio 3 模型、错误或无权限 Key、新加坡与 Workspace 专属 Host、接近 10 秒
  输入、各阶段取消、云端中途断网、闹钟抢占及长时间连续会话尚未完成本轮专项实机矩阵；
  相应输入与状态边界由主机测试覆盖，不在本版中宣称已经完成目标板验证。

## [0.19.0] - 2026-08-29

### Changed

- 设置门户显示已保存的家庭 Wi-Fi 名称，并可直接更换网络；新凭据只在内存中
  验证，取得 IPv4 后才写入 NVS。验证失败保留旧配置和门户，成功后关闭门户并按当前
  电源状态恢复网络：`NORMAL` 重新连接并保持在线，`SAVING` 完成本次主动校时后关闭
  无线；全过程不重启设备。密码不回传、不预填也不记录；清除 Wi-Fi 仍是独立确认的
  危险操作。
- 换网期间设备屏幕显示验证和保存结果；设置热点页将 `BOOT` 操作准确标为关闭，避免
  与撤销已经保存的设置混淆。
- `NORMAL` 在取得家庭网络 IPv4 后保持 STA 连接，并使用 ESP-IDF
  `WIFI_PS_MIN_MODEM` 降低关联空闲功耗；断线按 1、5、15 分钟退避，之后每 15 分钟
  重试。`SAVING` 不自动联网，手动校时、在线更新和图库下载只建立有时限的临时连接。
- 首屏 Wi-Fi 图标继续只表示当前 STA 已取得 IPv4；“状态”页将当前 Wi-Fi 链路与最近
  时间同步拆开显示，避免把已保存网络、互联网可用性或历史同步误写成当前连接。
- 首个有效温湿度样本立即显示对应的三态环境表情；读取失败时先执行一次传感器恢复重试，
  重试仍失败则保留最近有效数值和表情。最近有效样本未满 180 秒时“状态”页标记
  `STALE`，满 180 秒后首屏才改用占位符并隐藏表情；日常数值使用近一分钟有效样本的
  中位数，并只在可见精度变化时刷新。

### Development

- 新增易失的语音可靠性统计：每次短会话记录结果、指令编号、置信度、采集与 worker 耗时、
  内部 RAM、PSRAM、最大连续块、任务栈余量和 CPU 性能锁结果；USB `GET_VOICE` 返回汇总，
  `RESET_VOICE_DIAG` 只清空本次启动的计数。统计不保存、输出或传输原始 PCM。
- 新增会先确认服务就绪、预热、清零并逐项关联结果的 WSL/Windows 串口采集工具，以及
  固定 50 次实机测试矩阵；扩充四槽 TDM 单声道提取、
  统计聚合、BOOT 取消及闹钟在等待、监听、识别和结果阶段抢占的主机测试。

### Verified

- 用户通过测试通道依次验收 `0.19.0-dev.1` 和 `0.19.0-dev.2`：`NORMAL` 下家庭 Wi-Fi
  保持连接且首屏图标与实际链路一致，原有页面、离线语音和设置继续正常；温湿度与三态
  表情在日常运行中稳定显示。
- 正式 OTA 为 1,945,696 bytes，SHA-256 为
  `023b0be722f577724094fe77cc0e64ccabd190d4e4752e8adecbee15d61aa4ce`；模型为
  2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；Factory 为
  8,560,811 bytes，SHA-256 为
  `e682ed89373a02c8935a86dd5de47a9d65df7eba310571902a21cdee93f2c8a4`。
- 正式 OTA 与已实机验收的 `0.19.0-dev.2` 长度相同，仅 75 bytes 的版本、构建元数据、
  ELF 摘要和镜像摘要不同；模型逐字节一致。
- Wi-Fi 更换失败、SHTC3 连续故障的 `STALE` / 180 秒过期边界、固定 50 次语音矩阵及
  外部仪表功耗未做本轮专项实机验证；相应状态边界由主机测试覆盖，不在本版中宣称已经
  完成硬件故障注入或统计基线。

## [0.18.0] - 2026-08-28

### Added

- 新增始终生效的自动低电规则：冷启动不高于 20% 时进入 `SAVING`，运行期使用
  20% / 25% 滞回和连续样本确认；用户无需选择自动模式，也不能用普通设置关闭低电规则。
- 新增“手动提前省电”布尔偏好，默认关闭。它只允许用户在电量尚未触发阈值时提前进入
  `SAVING`，关闭手动请求不等于强制 `NORMAL`。
- `SETTINGS` 页新增设备端快捷切换：按住 `BOOT` 2 秒即可切换手动提前省电开关，
  保存后立即尝试热应用，无需开启设置热点；只有应用完成才显示成功，暂时无法应用时会
  提示等待重试。按住 `KEY` 3 秒仍保留完整设置门户入口。
- 检测到 USB 数据主机时立即使用 `NORMAL`，不再检测到主机后重新按新鲜电量采样
  判断；原板未将充电芯片状态或 VBUS 接入 ESP32，因此普通充电器、充电宝和纯充电线仍按
  电量判断，不把电压变化推测为充电状态。
- 新增 ESP-IDF 动态频率调节：CPU 频率范围配置为 80—240 MHz；实际生效为
  `NORMAL` 时锁定 240 MHz，实际生效为 `SAVING` 时允许系统在范围内按负载调节。
  本版明确不启用 Light-sleep 或 Deep-sleep。
- 新增用户主动发起的中文离线语音：在“语音”页按住 `KEY` 2 秒并松开后开始最长 5 秒
  的本地识别，监听时可用 `KEY` 提前结束、用 `BOOT` 取消。首版支持主界面、日历、
  图片、状态和设置的安全页面导航，以及取消当前会话。
- 新增独立的 3 MiB `model` 区域和版本化 `-model.bin` 发布附件。固定使用 ESP-SR 2.4.7
  中文 MultiNet5 模型，不启用 AFE、WakeNet、常驻 VAD、唤醒词或云端识别。
- `update-app.sh` 支持在同一次 USB 写入中安装应用与模型，同时保留 NVS、Wi-Fi 和设备
  偏好；Factory 固件包含应用、新分区表和同一模型。

### Changed

- 系统中心由“状态 → 音频 → 设置 → 在线更新”调整为“状态 → 语音 → 设置 → 在线更新”；
  原扬声器与双麦克风回环测试保留为开发和维护诊断，不再占用日常实体按键入口。
- 首屏 Wi-Fi 图标改为只表示当前家庭 STA 已取得 IPv4；未连接、连接中、失败、配网
  SoftAP 以及网络任务完成后均留空。校时历史仍由“状态”页显示，固定图标槽位保持日期与
  电池布局稳定。
- `SETTINGS` 页改为显示实际状态与主要原因：`NORMAL`、`NORMAL | USB`、
  `SAVING | MANUAL` 或 `SAVING | LOW BAT`。决策优先级为 USB 数据主机、手动提前省电、
  自动低电；不再显示或保存旧三档策略，也不提供强制 `NORMAL`。
- 运行状态切换改为原地热应用显示刷新、采样、自动联网和 CPU 策略，不重启设备；手动
  省电请求和自动低电规则共同决定实际状态，USB 数据主机始终强制 `NORMAL`。
- `SETTINGS` 页底部提示拆为导航与操作两行，分别说明普通页面切换，以及设备端手动省电
  切换和设置门户入口。
- 设备偏好升级到 schema v6，并使用 `manual_saving_requested` 与新的
  `cfg6_a` / `cfg6_b` 双槽。schema v5 的 `AUTO`、`NORMAL` 映射为手动关闭，`SAVING`
  映射为手动开启；schema v1—v4 的 `NORMAL` / `SAVING` 分别映射为关闭 / 开启，其他
  偏好保持不变。
- 电池百分比明确为 GPIO4 端电压估算，而非真实荷电状态；插线抬升与拔线回落不得被描述
  为实际容量瞬间变化，也不作为普通充电器或充电状态的可靠证据。确认电脑 USB 数据主机
  时，首屏以 `USB` 替代百分比，“状态”页只显示 `USB` 与端电压。

### Security

- 离线语音只在用户完成长按并松开后临时采集；原始 PCM 只存在于易失内存和识别器状态，
  不写入 Flash、NVS、microSD、日志或网络，结束、取消和失败后清理。
- 语音白名单不允许安装固件、清除 Wi-Fi、开启设置热点、删除图片、修改设置、切换省电
  或控制闹钟；未知、低置信度、缺失目标和迟到结果均不执行动作。
- 应用启动时完整校验模型大小和 SHA-256；模型缺失或损坏时显示 `VOICE UNAVAILABLE`，
  其他本地功能继续运行，不自动联网下载模型。

### Development

- 新增 STA 链路状态机和主机测试，覆盖取得、丢失和恢复 IPv4、主动停止，以及停止后
  迟到 `GOT_IP` 不得恢复连接状态；USB `GET_NETWORK` 同时输出当前 STA 连接证据。
- 扩充设置、运行期电源规则和记录迁移主机测试，覆盖布尔表单与 schema v6 编解码、
  schema v1—v5 映射、冷启动边界、20% / 25% 滞回、连续读数确认、无效电量、手动请求和
  USB 数据主机优先级。
- 新增语音会话状态机、指令策略、页面环、会话编号去重和迟到结果丢弃测试；构建流程固定
  ESP-SR、esp-dsp、dl_fft、cJSON 与模型摘要，并校验应用/模型分区、Factory 模型偏移和
  app+model USB 写入参数。

### Verified

- 实机确认自动低电规则无需额外选择模式，`SAVING` 隐藏秒数，插线与拔线后的估算电量
  变化符合 GPIO4 端电压边界；用户完成操作和重启观察后确认功能正常。
- 通过固定的 USB `update-app.sh` 流程一次写入应用与模型，Flash 哈希校验通过，原有
  NVS、Wi-Fi 和设备偏好保留；屏幕与语音页正常启动。
- 用户实机确认“打开日历”“查看状态”“回到主页”均能正确识别并打开对应页面，活动会话
  短按 `BOOT` 可以取消且不执行旧结果。
- 正式 OTA 为 1,927,968 bytes，SHA-256 为
  `67cfeae09efc5bb078e6a806870f9a971fb5c41b8722a7f1cf4ac60fcca3a2e5`；模型为
  2,203,819 bytes，SHA-256 为
  `29e156e606a46114b19a3e2f56406bc7045894743ae1e623d0b9e4c5ed1486bf`；Factory 为
  8,560,811 bytes，SHA-256 为
  `121615af71d3fe3ef09e3be03c9e2d8cba8ef4eb4029419026e16f4ae7859489`。
- 正式 OTA 与已实机验收的 `0.18.0-dev.1` 长度相同，仅 74 bytes 的版本、构建元数据、
  ELF 摘要和镜像摘要不同；模型逐字节一致。
- 外部仪表功耗与续航、Factory 实机重装、缺模型应用 OTA 降级、闹钟分阶段抢占、连续
  会话内存趋势、远距离/多噪声识别率和低电语音峰值未做专项实机验证，不在本版中宣称
  已完成这些结论。

## [0.16.0] - 2026-08-26

### Added

- 图片页新增设备端单张删除：按住 `KEY` 2 秒只进入当前图片的确认页，
  完整松开后需再短按 `KEY` 才写卡，`BOOT` 或超时可取消；失败时保持原目录，
  成功删除后显示下一张，删除末张后回到首张，删除最后一张图片后隐藏图片页。

### Changed

- 图片页 footer 分为导航和删除提示两行；长按 1 秒后显示剩余时间，删除执行与结果使用
  独立状态页，操作完成后无需重启设备。
- 删除任务改为异步写卡，并与设置门户导入、删除和图库安装共用存储卡互斥边界；写卡期间
  页面导航暂停，RTC、闹钟调度和主循环继续响应。

### Development

- 新增图片删除确认状态机、精确目录修订号与文件名快照、异步结果状态，以及长按释放门、
  二次确认、超时、目标变化和删除后选择策略的主机测试。

### Verified

- `0.16.0-dev.1` 已通过隔离的测试通道从 v0.15.0 完成在线更新；用户确认在线升级与本版
  功能未见问题，作为 OTA 和图片删除正常路径的整体冒烟验收。
- 固定依赖、仓库边界、许可材料、全部主机测试和 ESP-IDF v5.5.3 正式构建通过。慢卡、
  只读卡、强制写入失败及写卡期间触发闹钟未逐项进行破坏性实机验证；删除状态机、目标
  快照和选择策略已有主机测试，SDMMC、真实介质联动及闹钟抢占不在集成主机测试覆盖范围。

## [0.15.0] - 2026-08-25

### Added

- 设置门户新增可选的 microSD 图片管理：手机浏览器在本地将 JPEG/PNG
  按比例适配到顶部 400 × 250 可见区，生成、预览 400 × 300 PBM P4，
  只在用户确认后写入存储卡；原始图片不会离开手机。
- 新增公共演示图导入：用户确认后，设备可使用已保存的 Wi-Fi 从
  `mcu.taifua.com` 的固定 HTTPS 清单下载并校验 6 张公共演示图。
- 多图图片页支持短按 `KEY` 循环查看下一张，并在底部显示当前位置；用户选中的图片
  会一直显示并跨重启保留，多张图片仍共用一个日常图片页。
- 设置门户新增单张图片预览、前后浏览、设为当前和二次确认删除；删除当前图片后选择
  相邻图片，删除最后一张后图片页自动隐藏。

### Changed

- microSD 启动扫描改为将最多 32 张完整校验的 PBM/BMP 缓存到 PSRAM；
  无卡、挂载失败或没有有效图片时仍隐藏图片页，不显示启动错误也不影响
  任何核心功能。
- 普通展示期间保持 microSD 只读；只有用户确认的图片导入或单张删除会临时以写方式
  挂载存储卡。导入使用有界临时文件、完整格式校验、SHA-256 内容摘要、
  内容派生文件名和去重；服务端演示图另与清单摘要比对。固件不自动格式化、覆盖或
  清理已有文件；用户明确确认的单张删除只接受运行时已校验文件，失败时保持原目录。
  导入、选择和删除完成后直接安全更新运行时图片缓存。
- 移除普通设置、恢复偏好、手机图片导入、演示图集安装和清除 Wi-Fi 的
  整机重启。设置在主任务中热应用，图片写入后立即可用；清除 Wi-Fi 会关闭
  设置热点并原地进入配网。只有本地或在线 OTA 为切换应用分区自动重启。

### Development

- 新增图片导入与删除事务、图集清单严格解析、摘要与大小校验、图片去重、
  手动选择持久化、运行时缓存发布、网络会话原子移交以及设置兼容迁移的主机测试。

### Security

- 设置门户的图片选择、删除与写入沿用每次会话随机令牌和互斥事务；图片名称经过严格
  白名单、缓存精确匹配和安全路径构造，网页预览使用有界快照，不暴露内部缓存生命周期。
- 公共演示图只接受固定 HTTPS 主机与严格清单，逐张核对长度和 SHA-256；图片导入使用
  临时文件、刷盘、重新解码和内容派生文件名，失败时不覆盖已有图片。

### Verified

- `0.15.0-dev.4` 已通过隔离的测试通道完成在线更新；用户确认屏幕、按键和既有功能正常，
  多图 `KEY` 切换、重启后保留选择、设置门户预览/选择/单张删除均符合设计。
- 16 GB FAT32 microSD 的手机本地导入、公共演示图下载、运行时图片发布，以及普通设置、
  图片管理和清除 Wi-Fi 不重启设备的正常路径已完成实机验证。
- 固定依赖、仓库边界、许可材料、全部主机测试和 ESP-IDF v5.5.3 正式构建通过；正式
  OTA 与已验收候选大小一致，仅 77 bytes 的版本和构建元数据不同，运行时代码与配置一致。

## [0.14.0] - 2026-08-23

### Added

- 新增一个完全离线工作的本地闹钟：可在设置门户配置启用状态、`HH:MM` 时间和每周重复
  日期；`NORMAL` 与 `SAVING` 均由 PCF85063 本地时间触发，不依赖 Wi-Fi 或 NTP。
- 到点后显示高优先级大字提醒并循环播放三音提示；短按 `BOOT` 停止，首次响铃可短按
  `KEY` 延后 5 分钟，单次最长响铃 60 秒。

### Changed

- 设备偏好 schema 升级到 v3，并从 v2 双槽记录迁移；原有电源模式、时区、温标、播放
  音量和更新通道保持不变，新增闹钟默认关闭。
- RTC 完整读写事务增加互斥保护；闹钟启用时即使在 `SAVING` 或设置门户运行期间也至少
  每秒检查一次 RTC，同时避免与手机校时交错。
- 扬声器提示和音频诊断改为共用单一音频 worker；闹钟会先安全取消临时音频诊断，再接管
  ES8311/I²S，麦克风不可用时仍可播放闹钟。音频诊断的提示音与回放统一使用设置音量，
  `0%` 时保持静音但仍完成双麦克风分析。
- 设置 schema 标记缺失或损坏时，按当前双槽、v2 双槽、v1 字段的顺序恢复已有偏好，再
  迁移到 v3，避免可用设置被默认值覆盖。

### Development

- 新增闹钟调度、同次触发持久化记录和按键重新武装的纯逻辑测试，覆盖每周重复、同日去重、
  RTC 无效、日期回拨、错过分钟不补发、5 分钟单次延后、60 秒上限、计时回绕和记录损坏。

### Verified

- `0.14.0-dev.1` 已通过开发者测试通道完成在线更新；用户按实机验收步骤确认闹钟规则保存、
  到点提醒界面与提示音、首次短按 `KEY` 延后 5 分钟、随后短按 `BOOT` 停止均有效。
- 固定依赖、仓库边界、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 正式构建通过。
- 正式 OTA 与已实机验收的候选大小一致，仅 77 bytes 的版本与构建元数据不同；Factory
  前缀及其内嵌应用与对应 OTA 镜像逐字节一致。

## [0.13.0] - 2026-08-23

### Added

- 首屏新增舒适、一般和需调整三态环境提示，以自绘黑白线稿脸替换原心形分隔；无有效或
  尚未稳定的传感器数据时不显示表情。

### Changed

- 舒适度同时按温度和湿度的较差状态判定，并使用约一分钟读数窗口、边界滞回以及变差快于
  恢复的持续时间，避免 SHTC3 瞬时波动造成表情反复切换；临时读取失败只隐藏表情，不
  丢弃此前已经稳定的状态。

### Development

- 新增独立的环境舒适度纯逻辑模块和主机测试，覆盖阈值边界、两项取较差结果、启动稳定、
  异常读数、NORMAL/SAVING 采样间隔、滞回与状态切换延迟。

### Verified

- `0.13.0-dev.1` 已通过隔离的测试通道完成在线更新；用户确认首屏显示正常，当前约
  31 °C、73% RH 的环境按设计显示“需调整”皱脸。
- 固定依赖、仓库边界、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 候选构建通过。
- 正式 OTA 与已实机验收的候选大小一致，仅 76 bytes 的版本与构建元数据不同；Factory
  前缀及其内嵌应用与对应 OTA 镜像逐字节一致。

## [0.12.0] - 2026-08-23

### Added

- 新增 microSD 图片页：使用板载 1-bit SDMMC 在后台读取 FAT32 存储卡，
  从固定目录选择第一张完整校验通过的 400 × 300 PBM P4 或受限 1-bit BMP 图片，并将
  解码结果缓存到 PSRAM。
- 新增 `tools/rlcd-image.py`，可严格检查候选图片，或将常见图片按比例适配到顶部
  400 × 250 可见区并转换为规范 PBM P4。

### Changed

- 日常页面环在有效图片存在时扩展为“首屏 → 月历 → 图片 → 首屏”；无卡、目录缺失或
  图片无效时自动隐藏图片页，现有离线时钟与系统中心不受影响。
- 图片页固定覆盖底部 50 像素，以统一的 `BOOT: PAGE | KEY: SYSTEM` 提示保留可预测的
  实体按键语义。

### Development

- 新增目录候选边界、稳定排序、PBM/BMP 格式、像素极性、截断输入和条件页面环的主机
  测试，并记录 FAT32、固定目录、应用层只读访问与物理关机插拔约定。

### Verified

- `0.12.0-dev.1` 通过隔离的测试通道完成 OTA 安装；无卡时正常进入首屏，`BOOT` 保持
  “首屏 → 月历 → 首屏”，原有本地功能不受影响。
- 16 GB FAT32 microSD 实机挂载为 14,910 MiB；目录缺失时图片页按设计隐藏。写入严格
  校验的 400 × 300 PBM P4 后，用户确认图片方向、黑白极性、底部操作区和页面交互均
  正常。
- 固定依赖、仓库边界、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 正式构建通过。
  正式 OTA 与实机候选大小一致，仅 76 bytes 的版本与构建元数据不同；受限 BMP
  与候选数量上限由主机测试覆盖，非 FAT32、损坏文件系统及挂载失败尚未逐项实机制造。

## [0.11.0] - 2026-08-23

### Added

- 新增带结构版本的设备偏好设置，使用独立 NVS 命名空间保存运行模式、固定 UTC 偏移、
  摄氏/华氏温标和语音回放音量，普通 OTA 与断电不会清除。
- 新增统一的本地“设备设置”门户：可修改偏好、使用手机时间校准 RTC、恢复默认值、清除
  Wi-Fi 配置，并保留本地 OTA 上传入口。
- 新增 `SAVING` 运行模式：隐藏秒数并对齐整分钟刷新首屏，降低 RTC、温湿度与电池采样
  频率，停止开机/周期自动校时和后台更新检查；手动联网功能保持可用。
- 新增默认关闭的开发者测试通道。正式固件默认只检查稳定版，用户可在设置门户主动加入
  灰度；关闭后立即恢复检查稳定版且不会自动降级，在线安装仍需设备实体按键确认。

### Changed

- `KEY` 系统中心由六页收敛为“状态 → 音频 → 设置 → 在线更新”四页；状态页合并时间、
  RTC 备用电池、传感器、电池与网络校时摘要，本地 OTA 和 Wi-Fi 维护移入设置门户。
- `SETTINGS` 页新增 `PORTAL` 功能提示，直接标明配置、RTC、Wi-Fi 与本地
  OTA 均从此进入，避免页面收敛后误以为维护功能已被删除。
- 默认 `NORMAL` 模式保持既有显示、采样和自动联网行为。
- 设备偏好 schema 升级到 v2；从 v1 更新时保留运行模式、时区、温标和音量，并将新增的
  更新通道初始化为稳定版。

### Security

- 设置门户继续使用五分钟临时热点和随机 WPA2 密码，并为写操作增加每次会话随机令牌；
  设置写入、本地 OTA、重启及维护操作互斥，恢复默认值和清除 Wi-Fi 均需单独确认。
- 五分钟窗口改为不可续期的会话截止；截止后不再接受新操作，已取得互斥的短设置事务会在
  有限清理窗口内完整结束，本地 OTA 则在接收、写入与切换启动槽前持续复核截止时间并安全
  中止未完成镜像。
- 设备偏好改用带 CRC32 和递增代际的双槽记录；迁移先建立两份有效记录再切换 schema，
  保存中断或单槽损坏时可回退到最近一份完整设置，并在后续启动修复缺失或损坏的另一槽。

### Development

- 新增设置模型、省电策略、分钟边界、手机校时、门户令牌与并发策略的主机测试。

### Verified

- `0.11.0-dev.2` 通过隔离的测试通道提供并完成实机验收；用户确认四页系统中心、设置门户、
  页面与实体按键功能正常。
- 正式 OTA 与实机候选大小相同，仅 76 bytes 的版本、构建时间、ELF 摘要和镜像校验元数据
  不同；运行时代码与配置一致。
- 固定依赖、仓库边界、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 完整构建通过。

## [0.10.0] - 2026-08-22

### Added

- 新增 `ONLINE UPDATE`：通过 HTTPS 清单检查对应通道的最新版本，按住 `KEY` 2 秒检查或
  进入 `REVIEW`，确认页再次按住 `KEY` 3 秒后才下载、校验并写入未运行 OTA 槽。
- 网络校时成功后自动检查一次更新，只记录 `UP TO DATE`、`UPDATE AVAILABLE` 或失败
  结果，不切换页面、不下载且不静默安装；无网络时继续保留全部离线功能。
- 新增在线更新检查、确认、下载进度、校验和结果画面，以及清单解析、SemVer、URL 与网络
  会话策略的主机测试。

### Changed

- 系统中心从五页扩展为六页，将原“关于与更新”拆分为职责单一的 `ONLINE UPDATE` 和
  `LOCAL UPDATE`；本地更新仍通过随机密码热点工作，作为无互联网时的维护入口。
- 构建版本可用 `RLCD_PROJECT_VERSION` 覆盖；带预发布标识的版本读取 `testing.json`，
  正式版本读取 `stable.json`。

### Security

- 在线更新使用 ESP-IDF CA 证书包验证 HTTPS，严格检查清单的项目、硬件、通道、版本、
  最低可更新版本、固定下载主机、镜像大小和 SHA-256；安装前重新读取清单并确认目标未变。
- 清单与 OTA 下载只接受目标地址直接返回的 HTTPS `200` 响应，禁用并拒绝 HTTP 重定向，
  防止固定主机校验被跨主机或降级跳转绕过。
- OTA 镜像同时校验 Content-Length、应用项目名、应用版本、ESP 镜像完整性和 SHA-256，
  全部通过后才切换启动槽；同版本、降级、截断响应和无效镜像均被拒绝。

### Development

- 发布素材新增在线更新与本地更新页面效果图，开发文档补充 `testing.json`、`stable.json`
  和 v0.9.0 首次迁移到 v0.10.0 的流程。

### Verified

- 先通过设备本地更新页面安装 `0.10.0-dev` OTA 候选，再由该候选从 `testing.json`
  检查到 `0.10.0-rc.1`；版本发现和 `REVIEW` 确认页面均按设计工作。
- 实机完成在线连接、实体按键 3 秒安装确认、实际下载进度、镜像校验、切换启动槽和自动
  重启；设备随后运行 `0.10.0-rc.1`，用户确认页面、按键和现有功能均正常。
- 本次验证覆盖在线更新正常路径；重定向、错误证书、错误摘要、传输中断、降级和故意断电
  回滚等错误边界尚未在实机强制触发。

## [0.9.0] - 2026-08-22

### Changed

- 改为离线优先启动：RTC 是否有效都直接保留首屏和本地页面，自动 Wi-Fi 连接、NTP 失败
  与后台重试不再显示全屏状态或周期性配网页；RTC 无效时稳定显示时间和日期占位符。
- 已保存网络连接失败后不再自动开放配网热点，也不清除凭据；Wi-Fi 离线、NTP 不可用和
  网络服务错误分别记录，后台按 1、5、15、60 分钟退避，恢复后回到 24 小时校时周期。
- 首次或主动重配网的二维码显示 60 秒后恢复首屏，临时热点最多开放 5 分钟后关闭；需要
  离线使用时可按 `BOOT: OFFLINE` 立即跳过二维码；需要再次配网可重启或从“Wi-Fi 维护”
  页明确操作。
- “网络与时间”页现在可在离线退避期间长按 `KEY` 立即重试，不必等待下一后台周期。
- 没有家庭 Wi-Fi 配置时仍可在配网窗口结束后使用随机密码本地 OTA；升级网页继续只在
  用户从“关于与更新”页长按后开放，不依赖互联网。

### Development

- 新增独立的网络退避和自动配网页面策略，并以主机测试覆盖边界，防止网络状态重新阻塞
  日常界面。

### Verified

- 实机确认无 Wi-Fi 配置时可退出配网页并持续使用 RTC、首屏、月历和本地功能；临时热点
  5 分钟后按期关闭，配网页不会再次抢占首屏。
- 重新配网后自动完成 NTP 校时，后续重启可使用保存的配置恢复联网，不再进入配网页。
- 固定依赖、仓库边界、许可材料、全部主机测试和 ESP-IDF v5.5.3 正式构建通过。

## [0.8.0] - 2026-08-21

### Added

- 新增 ES8311 扬声器与 ES7210 双麦克风音频基础组件；固定使用 24 kHz、16-bit，并仅从
  仓库外的微雪固定提交中编译 Espressif `esp_codec_dev` 所需子集。
- 系统中心新增“音频”页：按住 `KEY` 2 秒播放双音提示，然后从 ES7210 MIC1/MIC3
  对应的 TDM 时隙临时采集最多 5 秒语音，并自动回放信号更强的一路；录制和回放期间
  显示两路去直流 RMS 相对强度与进度。
- 音频测试改为非阻塞后台状态机；录制或回放时短按 `KEY` 可提前结束当前阶段，任一活动
  阶段短按 `BOOT` 可取消。原始语音只存在 PSRAM，播放、取消或失败后立即清除。
- USB 命令新增 `GET_AUDIO`，返回 codec 就绪、测试阶段、时长、回放来源和两路强度，不
  输出音频数据。

### Changed

- 系统中心从四页扩展为五页：设备健康、网络与时间、音频、Wi-Fi 维护、关于与更新；
  日常首屏和 `BOOT` 页面环保持不变。
- 实机确认回放链路正常但人声略小后，仅将语音回放提高约 5 dB；已验证的双音提示音音量
  和数字峰值限幅保持不变。

### Verified

- 去直流 RMS、通道选择、活动阈值与饱和映射主机测试通过；ESP-IDF v5.5.3 可完整编译
  `0.8.0` Factory/OTA 镜像。
- `0.8.0-dev` OTA 候选已在 ESP32-S3-RLCD-4.2 实机启动；用户确认双音、最长 5 秒录制、
  `KEY` 提前停止、`BOOT` 取消、自动语音回放和各页面状态均正常。回放人声可以听到但
  稍小；提高约 5 dB 后再次实机试听，用户确认效果可用。两路强度定量校准与长时间音频
  稳定性尚未测试。

## [0.7.0] - 2026-08-21

### Added

- 新增 3 MiB `ota_0`/`ota_1` 双应用槽、8 KiB OTA 选择数据和 Bootloader 应用回滚；新
  镜像写入未运行槽并完整校验后才切换，首次启动完成基本诊断后确认。
- “关于与更新”页按住 `KEY` 3 秒可开启 5 分钟本地升级窗口：设备生成带唯一编号的临时
  WPA2 热点和 8 位随机密码，手机扫码连接后在本地网页上传 OTA 固件。
- 新增固件接收百分比、镜像校验、成功重启、失败、超时和取消画面；等待上传时可短按
  `BOOT` 取消，开始写入后实体按键不会中断操作。
- 新增 `-factory.bin` 与 `-ota.bin` 两类发布物，以及保留 NVS 的
  `scripts/update-app.sh` 串行更新后备流程和独立升级文档。
- 设备健康页新增 `RTC BACKUP` 状态：在网络启动前读取 PCF85063 停振标志，并通过跨启动
  NVS 时间标记显示 `UNTESTED`、`VERIFIED` 或 `FAILED`，避免 NTP 掩盖断电保持结果。

### Changed

- Factory 完整镜像只用于首次安装、旧分区迁移和恢复，仍会覆盖 NVS；网页 OTA 和串行
  应用更新不再清除已保存的家庭 Wi-Fi 配置。
- “关于与更新”页把 USB 烧录改为恢复提示，并将本地升级作为主要更新入口；构建脚本会
  验证双 OTA 分区并明确生成 Factory/OTA 两种文件，烧录脚本拒绝混用镜像类型。
- 配网与升级二维码统一采用标准白底黑码、完整静区和整数像素倍率。
- NVS 初始化移入项目公共存储组件，网络凭据和 RTC 判定继续使用互相独立的命名空间；
  Wi-Fi 重置不清除 RTC 断电验证结果。

### Security

- 升级 HTTP 服务只在实体长按后临时开放于随机密码保护的本地热点；一次只允许一个客户
  端，日志不记录密码，超时或退出后停止 HTTP 和 Wi-Fi。
- 上传端检查长度、ESP 镜像头、应用描述和项目名；无效或中断的上传不会修改当前启动槽。

### Verified

- 页面长按动作、升级状态分类、进度计算和 RTC 备用电池判定策略主机测试通过；自定义
  分区表生成并确认包含 `otadata`、`ota_0` 和 `ota_1`，ESP-IDF v5.5.3 完整构建通过。
- 实机完成 Factory 分区迁移、重新配网、升级入口与取消、本地网页 OTA 接收与校验、自动
  重启以及 Wi-Fi/NVS 保留；更新后无需再次输入家庭网络凭据。
- RTC 备用电池状态实机覆盖 `UNTESTED`、断电停振后的 `FAILED`，以及 ML1220 充电后再次
  断电恢复为 `VERIFIED`；判定均在 NTP 恢复时间前完成。
- Bootloader 回滚配置、镜像首次启动确认和串行后备脚本已完成构建检查；故意失败镜像回滚
  与 `update-app.sh` 仍未单独执行实机破坏性测试。

## [0.6.0] - 2026-08-20

### Added

- 新增四页系统中心：设备健康、网络与时间、Wi-Fi 维护、关于与更新；统一标题、页码、
  内容网格和底部操作提示，并将硬件、联网、维护和版本信息按职责分开。
- 新增项目级产品界面与交互设计规范，明确“简约、清爽、明亮”的视觉方向、日常/系统
  两级信息架构、实体按键语义、危险操作确认和页面验收清单；根目录 `AGENTS.md` 只保存
  开发约束并链接该规范。
- 新增上下文长按策略与主机测试：网络与时间页按住 `KEY` 2 秒即时校时，Wi-Fi 维护页
  按住 5 秒清除配置，其他页面长按不执行动作且不会误判为短按。

### Changed

- `BOOT` 现在只负责首屏与月历切换，或从系统中心返回首屏；正常运行时长按不再承载隐藏
  校时操作。`KEY` 负责进入和循环系统中心，两个按键的页面域与职责保持稳定。
- 月历和全部系统页统一在无操作 30 秒后返回首屏；按键交互、校时和 Wi-Fi 重置确认期间
  暂停超时。
- README 效果预览改为每行两张，并同步新增四个系统页面的 400 × 300 黑底白字效果图。

### Verified

- 仓库边界、文档链接、许可材料、全部主机逻辑测试和 ESP-IDF v5.5.3 完整构建通过。
- `0.6.0-dev` 完整镜像在 ESP32-S3 实机从 `0x0` 写入并通过哈希校验；用户确认重新配网、
  时间恢复、页面导航和当前功能运行正常。
- 实机验收后仅把版本字符串更新为 `0.6.0`；正式构建再次通过依赖、许可、主机测试和
  ESP-IDF 完整构建检查，镜像大小与 SHA-256 记录在
  [`dist/v0.6.0/README.md`](dist/v0.6.0/README.md)。

## [0.5.0] - 2026-08-20

### Added

- 板载 `BOOT`（GPIO0）运行时操作：短按在时钟首屏、当月月历和固件信息页之间循环；
  月历突出今天并显示当天农历、星期和时间，固件页显示运行版本、最新 Release 二维码和
  USB 烧录按键提示。
- 正常运行时长按 `BOOT` 2 秒可立即唤醒网络任务执行一次 SNTP 校时，写入并回读 RTC 后
  显示结果；未配置 Wi-Fi 或任务忙碌时只显示不可用提示。
- 可主机测试的页面状态机，统一三个主页面、设备状态页、15/30 秒超时、返回和循环规则；
  新增公历月份天数及首日星期计算与边界测试。

### Changed

- 输入驱动同时初始化 `BOOT`（GPIO0）与 `KEY`（GPIO18），两者继续使用独立消抖和长按
  阈值；`KEY` 的状态页及长按 5 秒重配网行为保持不变。
- 网络任务的 24 小时重校和失败重试等待可被手动同步请求安全唤醒；手动同步失败不会立即
  开放配网热点或删除原有凭据。
- 新增页面均使用 400 × 300 黑底白字布局；固件页二维码采用白底黑码和完整静区。

### Verified

- 主机测试增加 BOOT 2 秒长按、页面切换与超时、公历闰年和月份首日校验；仓库检查、
  许可检查和 ESP-IDF v5.5.3 完整构建通过。
- ESP32-S3 实机确认：短按 `BOOT` 可依次切换月历、固件信息页和首屏，次级页面可自动
  返回；Release 二维码、`KEY` 原有操作以及长按 `BOOT` 即时联网校时均正常。
- 正式发布镜像的大小与 SHA-256 记录在
  [`dist/v0.5.0/README.md`](dist/v0.5.0/README.md)。

## [0.4.0] - 2026-08-20

### Added

- 板载 `KEY` 运行时操作：短按进入设备状态页，再次短按或等待 15 秒返回首屏；状态页显示
  固件版本、RTC、温湿度传感器、电池、网络状态和本次启动最近一次 SNTP 校时。
- `KEY` 长按维护操作：按住 1 秒后显示倒计时，满 5 秒仅清除本项目 Wi-Fi 凭据并重启
  配网；中途松开会取消，不修改任何配置。
- 独立、可主机测试的按键消抖与短按/长按状态机，以及抖动、取消、阈值和单次触发测试。

### Changed

- 应用循环以 50 ms 采样 `KEY`，RTC 与网络状态仍按 1 秒周期更新，温湿度和电池原有
  低频采样周期保持不变。
- `BOOT` 保持为 ESP32-S3 下载模式按键，不承担运行时交互；日常状态和配网维护统一使用
  GPIO18 的板载 `KEY`。

### Development

- 新增轻量 GitHub Actions，自动检查仓库边界、本地文档链接、历史发布哈希和主机端逻辑
  测试，无需在 CI 中安装完整 ESP-IDF 工具链。

### Verified

- 按键状态机的抖动、短按、长按取消、5 秒阈值和单次触发主机测试通过；仓库检查、许可
  检查和 ESP-IDF v5.5.3 完整构建通过。
- ESP32-S3 实机确认：短按 `KEY` 可显示并退出状态页，15 秒自动返回正常；2 秒松开可
  取消重配网，持续 5 秒可清除 Wi-Fi 并自动重启，重新配网和 SNTP 校时正常。
- 正式发布镜像的大小与 SHA-256 记录在
  [`dist/v0.4.0/README.md`](dist/v0.4.0/README.md)。

## [0.3.0] - 2026-08-19

### Added

- 带每次会话随机 WPA2 密码的 SoftAP 配网模式，以及无需外部脚本和云服务的本地 HTTP
  配网页面。
- 家庭 Wi-Fi 凭据的项目独立 NVS 存储、输入边界检查和纯主机解析测试。
- 通过 `ntp.aliyun.com`、`cn.pool.ntp.org`、`pool.ntp.org` 获取中国标准时间，写入并
  回读验证 PCF85063；同步成功后关闭 Wi-Fi，每 24 小时重校。
- USB `GET_NETWORK` 和 `RESET_WIFI` 命令，以及配网、连接、校时和重试状态画面。
- 首行 Wi-Fi 状态图标，与电池共同组成第四组；完整图标表示本次启动已成功网络校时，
  下半层图标表示正在连接，斜杠图标表示尚未配置或最近一次连接失败。
- 临时配网热点名称、设备编号后缀和固定密码的 ESP-IDF Kconfig 选项；项目默认追加唯一
  设备编号，固定密码留空时保留每次会话随机密码。
- 配网画面的标准 Wi-Fi 加入二维码，包含中等级别纠错、四模块静区、特殊字符转义和文字
  SSID/密码/地址后备信息。
- lwIP、wpa_supplicant、HTTP Parser 和 QR Code Generator 的完整许可与归属材料。

### Changed

- USB 命令模块由仅负责 RTC 的 `rtc_time_sync` 重命名为职责更完整的 `usb_commands`；
  原有 `SET_TIME`、`GET_TIME` 和 `HELP` 行为保持兼容。
- RTC 无效时优先显示自动联网校时状态；USB 手动校时继续作为后备方案。
- 静态配网和网络状态画面只在状态变化时刷新，以降低全反射屏的无效刷新与功耗。
- 临时热点的默认随机密码缩短为 8 位大写字母与数字，并排除容易混淆的 `0/O/1/I`；仍
  使用 Wi-Fi RF 熵源提供的 40 位随机量。
- Wi-Fi 状态图标由 1 像素圆弧和双叉改为 18 × 14 像素实心图形和带留白边的单斜杠，
  与电池图标顶边、视觉重量和固定状态组占位对齐。

### Fixed

- Wi-Fi 二维码不再给 SSID 和密码额外包双引号，避免部分 Android 厂商解析器把引号
  当作凭据内容而无法连接临时热点。

### Security

- 家庭 Wi-Fi 名称和密码不会写入串口日志或仓库；配网成功后临时热点密码从应用状态中
  清除。
- Espressif QR Code 上游包装层的输入日志在编译时关闭，避免二维码原文中的临时热点
  密码进入串口输出。
- 当前 NVS 未启用静态加密，完整合并固件会覆盖 NVS 并清除网络凭据；相关限制已写入
  [自动配网与网络校时](docs/network-time.md)。

### Verified

- Wi-Fi 表单解码、最大长度、重复/缺失字段、控制字符、错误百分号编码，以及二维码字段
  和特殊字符转义测试通过。
- ESP32-S3 实机完成临时热点、本地配网页面、家庭 Wi-Fi 连接、SNTP 自动校时、RTC 写入
  和首屏 Wi-Fi 状态验收；修正载荷后，小米手机扫码加入临时热点通过。
- ESP-IDF v5.5.3 完整构建通过；正式发布镜像的大小与 SHA-256 记录在
  [`dist/v0.3.0/README.md`](dist/v0.3.0/README.md)。

## [0.2.0] - 2026-08-18

### Added

- 2000—2099 年离线农历换算和中文显示，包含闰月与日期边界测试。
- GPIO4 电池 ADC 采样、三倍分压还原、异常值滤波和 18650 电量估算。
- 可直接运行的 `./scripts/test.sh` 主机测试流程。
- Apache License 2.0 项目许可证、第三方软件/字体完整许可材料和安全报告说明。
- 可重复执行的许可核对与 Release 附件打包脚本。

### Changed

- 首屏改为日常仪表盘：等大显示 `HH:MM:SS`，并显示公历、中文星期、农历、温湿度和
  电池图标。
- 农历月日移入首行并去掉“农历”前缀；温湿度区域去掉文字标题，大时间在主区域居中。
- RTC 无效时移除时间下方提示；温湿度缩小字号并改用实心心形分隔。
- 首屏采用上下各 56 像素的对称三段网格；首行四组信息等间距，时间以整屏中心作光学
  居中，温湿度使用 20 像素字体并分别在左右半屏居中。
- 从首屏移除固件版本、I2C 地址、传感器 ID、硬件状态和 `Hello, world.`。

### Verified

- 农历算法对 2000-01-01 至 2099-12-31 共 36,525 个公历日期执行全范围转换和字段边界
  测试，并按香港天文台对照表核对代表日期、闰月与临界新月。
- ESP-IDF v5.5.3 主机测试和完整构建通过；v0.2.0 合并镜像大小 699,504 bytes，
  SHA-256 为 `d034c397aba9f79985bc7c187c24fb8db4e974b6b7ca7c0dcd0b724e1a21e389`。
- ESP32-S3 实机烧录、启动和新首屏布局验收通过；RTC 正常递增，SHTC3 读数正常，
  GPIO4 电池 ADC 实测约 4,125 mV / 93%，8 MB PSRAM 正常。

## [0.1.1] - 2026-08-17

### Added

- USB Serial/JTAG 命令：`SET_TIME`、`GET_TIME`、`HELP`。
- Windows 上海时间校时脚本，支持日期校验、星期计算和写后回读。
- 屏幕底部 `Hello, world.`。
- 可重复的依赖检查、构建、Windows COM 烧录和 RTC 校时流程。
- 已实机验证的 v0.1.1 完整合并固件。

### Changed

- RTC 无效时显示 `CONNECT USB TO SET RTC`。
- PCF85063 写入遵循停止计数、整块写入七个时间寄存器、恢复计数的顺序。

### Verified

- ESP32-S3 rev 0.2、16 MB Flash、8 MB PSRAM。
- PCF85063 地址 `0x51`，SHTC3 地址 `0x70`、芯片 ID `0x0887`。
- `RTC_SET_OK 2026-08-17 22:33:20 weekday=1`，随后时间正常递增。

## [0.1.0] - 2026-08-17

### Added

- 原生 ESP-IDF 5.5.3 工程。
- ST7305 全反射屏诊断时钟界面。
- PCF85063 RTC 读取与掉电失效检查。
- SHTC3 温湿度读取和 CRC 校验。
- 离线启动日志与硬件故障隔离。
